# Codex Windows／WSL 安裝文章製作紀錄

## 素材規則

- WSL 與 CLI 畫面來自 2026-07-24 實際操作紀錄。
- Windows／VS Code GUI 圖片以 Windows 實機截圖為準。
- 只收錄實測真正出現的警告與問題，不以假設錯誤補充篇幅。
- 錯誤必須先保留終端機或介面顯示的原文，再撰寫中文解釋。
- 未完成驗證的修正方式不得提前宣稱成功。

## 已取得素材

- VS Code Marketplace 搜尋 OpenAI 官方 Codex 插件。
- Windows PowerShell 尚未安裝 CLI 時的 `CommandNotFoundException`。
- VS Code 空白視窗送出訊息時的 `Unable to send message`。
- `File → Open Folder...` 操作畫面。
- WSL2 環境與工具版本。
- Codex CLI 隔離安裝輸出。
- 多份 Codex CLI 造成的 PATH 版本差異。
- VS Code 終端機輸入 `codex` 並成功開啟 CLI。

## 待完成

- 開啟專案後再次送出訊息，確認 `Unable to send message` 是否排除。
- 使用 VS Code 插件協助安裝 CLI 的完整過程。
- 複製真實錯誤訊息交給 AI、修正並再次驗證的錄影。
