---
import ogBanner from '../assets/banner-v2.png';
import githubLogo from '../assets/github-logo.svg';
import lineLogo from '../assets/line-logo.png';
import tookaLogo from '../assets/tooka-logo.svg';
import { NAV, LINKS, SITE, SERVICE_SITE, AUTHOR, PERSON_SCHEMA, pageBannerUrl, speakingBannerUrl } from '../consts';
import '../styles/global.css';

// 頁首導覽不放「首頁」：字標本身就是回首頁的連結，重複一次是浪費連結預算。
// 頁尾的網站導覽仍然列出完整 NAV（含首頁）。
const HEADER_NAV = [
  { href: '/about/', label: '關於' },
];
const CLAIM_INSTALL_HINT = '貼給你的 AI Agent，說「幫我安裝」';
const DROPDOWN_NAV = [
  {
    key: 'articles',
    label: '文章',
    href: '/blog/',
    subtitle: '從新手入門、工具安裝到社群自動化',
    previews: [
      {
        href: '/blog/codex-windows-wsl-install/',
        eyebrow: '工具安裝',
        title: 'Codex 新手安裝',
        description: '從 VS Code 插件開始，不用先學終端機操作。',
        image: '/images/blog/codex-windows-wsl-install.png',
        cta: '閱讀安裝教學',
      },
      {
        href: '/blog/threads-api-tutorial/',
        eyebrow: '社群自動化',
        title: 'Meta API 實戰',
        description: 'Threads、Instagram 申請與自動發文工作流。',
        image: '/images/blog/threads-api-tutorial.png',
        cta: '閱讀 API 教學',
      },
    ],
  },
  {
    key: 'resources',
    label: '領取',
    href: '/resources/',
    subtitle: '免費教學影片、開源工具與學習路線',
    previews: [
      {
        href: '/blog/why-i-built-tooka/',
        eyebrow: '我開發的工具',
        title: 'Tooka 社群圖文',
        description: '解決生圖常見的跑版、失真與文字亂碼，用對話生成 IG、Facebook 與 Threads 圖文。',
        image: tookaLogo.src,
        navIcon: tookaLogo.src,
        tookaIcon: true,
        cta: '領取',
        repo: 'https://github.com/js0980420/tooka',
      },
      {
        href: 'https://www.instagram.com/reel/DbwqIJkydIQ/',
        eyebrow: '提升開發效率 Skill',
        title: 'grill-me：別等 AI 做完才發現搞錯',
        description: '你以為 AI 聽懂了，做完才發現完全不是你要的。它會在動工前一路問清楚，把需求、選項和每個分支先確認好。',
        image: githubLogo.src,
        navIcon: githubLogo.src,
        external: true,
        cta: '領取',
        repo: 'https://github.com/mattpocock/skills',
      },
    ],
  },
  {
    key: 'services',
    label: '服務',
    href: `${SERVICE_SITE.url}/course/`,
    subtitle: '先看內容，再選適合你的合作方式',
    accent: true,
    previews: [
      {
        href: `${SERVICE_SITE.url}/course/`,
        eyebrow: '3 小時課程',
        title: 'AI 新手實戰課',
        description: '從工具安裝、專案規則到除錯解耦，帶你從 0 到 1 完成第一個專案。',
        image: new URL(pageBannerUrl('one-on-one')).pathname,
        cta: '查看課綱',
      },
      {
        href: `${SERVICE_SITE.url}/services/speaking/`,
        eyebrow: '演講與實作',
        title: 'AI 主題演講',
        description: 'AI Agent、社群自動化與對話式拍片，可依企業、協會或社群需求規劃。',
        image: new URL(speakingBannerUrl()).pathname,
        cta: '查看演講服務',
      },
    ],
  },
];

interface Props {
  title: string;
  description: string;
  /** 頁面專屬的 JSON-LD（Article、Service、FAQPage 等），會與全站 Person 一起輸出 */
  schema?: object[];
  ogType?: string;
  /** 社群分享預覽圖，預設用全站橫幅 */
  ogImage?: string;
  /** og:image 的像素尺寸與替代文字；自訂 ogImage（文章橫幅固定 1200×630）時沿用預設即可 */
  ogImageWidth?: number;
  ogImageHeight?: number;
  ogImageAlt?: string;
  /** 不公開頁面：禁止搜尋引擎索引（unlisted 文章用） */
  noindex?: boolean;
}

const {
  title,
  description,
  schema = [],
  ogType = 'website',
  noindex = false,
  ogImage: pageOgImage,
  ogImageWidth = pageOgImage ? 1200 : ogBanner.width,
  ogImageHeight = pageOgImage ? 630 : ogBanner.height,
  ogImageAlt = title,
} = Astro.props;
const ogImage = pageOgImage ?? new URL(ogBanner.src, SITE.url).href;
const canonical = new URL(Astro.url.pathname, Astro.site);

const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'WebSite',
      '@id': `${SITE.url}/#website`,
      name: SITE.title,
      alternateName: [SITE.titleEn, 'aixwang'],
      url: SITE.url,
      description: SITE.description,
      inLanguage: SITE.locale,
      publisher: { '@id': PERSON_SCHEMA['@id'] },
    },
    PERSON_SCHEMA,
    ...schema,
  ],
};
---

<!doctype html>
<html lang="zh-TW">
  <head>
    <!-- Google Analytics（GA4）流量統計：gtag.js 延到頁面載入完才注入，避免佔用首次渲染的主執行緒；
         事件在此之前先排入 dataLayer，載入後照樣送出。
         noindex 頁面（草稿／不公開頁）完全不輸出 GA 腳本，不會被統計 -->
    {!noindex && <script is:inline>
      // 只統計正式網站；localhost、區域網路 IP 與其他預覽網址完全不載入 GA4。
      // 自用裝置開一次 ?analytics=off 會寫入跨子網域 Cookie；?analytics=on 可恢復。
      // 一般不執行 JavaScript 的爬蟲本來就不會送 GA；自動化瀏覽器則額外排除。
      var analyticsHosts = ['aixwang.dev', 'www.aixwang.dev'];
      var analyticsControl = new URLSearchParams(window.location.search).get('analytics');
      if (analyticsControl === 'off') {
        document.cookie = 'aixwang_ga_opt_out=1; Max-Age=315360000; Path=/; Domain=.aixwang.dev; SameSite=Lax; Secure';
      } else if (analyticsControl === 'on') {
        document.cookie = 'aixwang_ga_opt_out=; Max-Age=0; Path=/; Domain=.aixwang.dev; SameSite=Lax; Secure';
      }
      if (analyticsControl === 'off' || analyticsControl === 'on' || analyticsControl === 'status') {
        var cleanAnalyticsUrl = new URL(window.location.href);
        cleanAnalyticsUrl.searchParams.delete('analytics');
        window.history.replaceState({}, '', cleanAnalyticsUrl.pathname + cleanAnalyticsUrl.search + cleanAnalyticsUrl.hash);
      }
      var analyticsOptedOut = /(?:^|;\s*)aixwang_ga_opt_out=1(?:;|$)/.test(document.cookie);
      var analyticsAutomation = navigator.webdriver === true || /HeadlessChrome|Playwright|Puppeteer|Selenium|Aixwang[^\s]*Bot/i.test(navigator.userAgent);
      if (analyticsControl === 'status') {
        document.addEventListener('DOMContentLoaded', function () {
          var disabled = analyticsOptedOut || analyticsAutomation;
          var notice = document.createElement('div');
          var copy = document.createElement('span');
          var title = document.createElement('strong');
          var detail = document.createElement('small');
          var close = document.createElement('button');
          notice.setAttribute('role', 'status');
          notice.setAttribute('aria-live', 'polite');
          notice.style.cssText = 'position:fixed;z-index:2147483647;top:max(16px,env(safe-area-inset-top));left:50%;display:flex;width:min(440px,calc(100vw - 32px));transform:translateX(-50%);align-items:center;gap:14px;padding:14px 16px;border:1px solid ' + (disabled ? '#78b894' : '#d38b8b') + ';border-radius:6px;background:#171615;color:#fff;box-shadow:0 16px 48px rgba(0,0,0,.28);font-family:system-ui,-apple-system,sans-serif';
          copy.style.cssText = 'display:grid;min-width:0;flex:1;gap:3px';
          title.style.cssText = 'font-size:15px;line-height:1.4;color:' + (disabled ? '#91d6ae' : '#ffb3b3');
          detail.style.cssText = 'font-size:12px;line-height:1.55;color:#d4d0cc';
          close.type = 'button';
          close.setAttribute('aria-label', '關閉 GA4 狀態提示');
          close.textContent = '×';
          close.style.cssText = 'width:34px;height:34px;padding:0;border:1px solid #5b5753;border-radius:50%;background:transparent;color:#fff;font-size:20px;cursor:pointer';
          title.textContent = disabled ? '✓ GA4：已排除' : '● GA4：仍在統計';
          detail.textContent = analyticsOptedOut
            ? '這個瀏覽器已設定排除，不會送出 GA4 資料。'
            : analyticsAutomation
              ? '已辨識為自動化瀏覽器，不會送出 GA4 資料。'
              : '這個瀏覽器目前會送出 GA4 資料。';
          close.addEventListener('click', function () { notice.remove(); });
          copy.append(title, detail);
          notice.append(copy, close);
          document.body.appendChild(notice);
        });
      }
      if (analyticsHosts.indexOf(window.location.hostname) !== -1 && !analyticsOptedOut && !analyticsAutomation) {
        var preconnect = document.createElement('link');
        preconnect.rel = 'preconnect';
        preconnect.href = 'https://www.googletagmanager.com';
        preconnect.crossOrigin = 'anonymous';
        document.head.appendChild(preconnect);

        window.dataLayer = window.dataLayer || [];
        window.gtag = function () {
          window.dataLayer.push(arguments);
        };
        window.gtag('js', new Date());
        window.gtag('config', 'G-91Z2F673C0');

        window.addEventListener('load', function () {
          var s = document.createElement('script');
          s.async = true;
          s.src = 'https://www.googletagmanager.com/gtag/js?id=G-91Z2F673C0';
          document.head.appendChild(s);
        });

        // 今日新增的互動入口統一送 button_click，以參數區分按鈕、位置與動作。
        // 搜尋文字會先在瀏覽器遮罩 Email、電話、URL 與帳號，再以 search_term 送出。
        function redactSearchTerm(value) {
          return String(value || '')
            .replace(/\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi, '[email]')
            .replace(/https?:\/\/\S+|www\.\S+/gi, '[url]')
            .replace(/(^|\s)@[a-z0-9._-]+/gi, '$1[account]')
            .replace(/(?:\+?\d[\d\s().-]{7,}\d)/g, '[phone]')
            .replace(/\s+/g, ' ')
            .trim()
            .slice(0, 100);
        }

        function trackButton(control) {
          if (!control) return;
          var label = control.getAttribute('data-ga-label') || control.textContent || '';
          var destination = control.href || control.getAttribute('data-copy-repo') || '';
          var parameters = {
            button_id: control.getAttribute('data-ga-button') || control.getAttribute('data-ga-submit') || 'unknown',
            button_label: label.replace(/\s+/g, ' ').trim().slice(0, 100),
            button_location: control.getAttribute('data-ga-location') || 'unknown',
            button_action: control.getAttribute('data-ga-action') || 'click',
            destination_url: destination,
            page_path: location.pathname,
          };
          window.gtag('event', 'button_click', parameters);
          // GA4 未註冊 button_id 自訂維度時，button_click 只能看總次數。
          // 另送 btn_<id> 原生事件，事件名稱最長 40 字元，可直接在事件報表看單顆次數。
          var buttonEventName = ('btn_' + parameters.button_id).replace(/[^a-zA-Z0-9_]/g, '_').slice(0, 40);
          window.gtag('event', buttonEventName, {
            button_label: parameters.button_label,
            button_location: parameters.button_location,
            page_path: parameters.page_path,
          });
          // 同步寫入 Directus，讓 CMS 能直接查看每顆按鈕的點擊明細與次數。
          // 不送搜尋文字、Email、IP 或 Cookie，只送畫面上既有的按鈕中繼資料。
          var cmsPayload = JSON.stringify(parameters);
          if (navigator.sendBeacon) {
            navigator.sendBeacon('/api/button-click', new Blob([cmsPayload], { type: 'application/json' }));
          } else {
            fetch('/api/button-click', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: cmsPayload,
              keepalive: true,
            }).catch(function () {});
          }
        }

        // 自訂事件：追蹤按鈕與關鍵轉換點擊（LINE 諮詢 / Tooka / YouTube）
        document.addEventListener('click', function (e) {
          var control = e.target.closest && e.target.closest('[data-ga-button]');
          if (control) trackButton(control);

          var a = e.target.closest && e.target.closest('a[href]');
          if (!a) return;
          var href = a.href || '';
          if (href.indexOf('line.me') !== -1) {
            window.gtag('event', 'line_click', { link_url: href, page_path: location.pathname });
          } else if (href.indexOf('/course') !== -1) {
            window.gtag('event', 'one_on_one_click', { link_url: href, page_path: location.pathname });
          } else if (href.indexOf('/services/speaking') !== -1) {
            window.gtag('event', 'speaking_click', { link_url: href, page_path: location.pathname });
          } else if (href.indexOf('tooka') !== -1) {
            window.gtag('event', 'tooka_click', { link_url: href, page_path: location.pathname });
          } else if (href.indexOf('youtube.com') !== -1 || href.indexOf('youtu.be') !== -1) {
            window.gtag('event', 'youtube_click', { link_url: href, page_path: location.pathname });
          }
        });

        // 透過 submit 追蹤搜尋送出，點箭頭與按 Enter 只會各記一次。
        document.addEventListener('submit', function (e) {
          var form = e.target.closest && e.target.closest('form[data-ga-submit]');
          if (!form) return;
          var inputSelector = form.getAttribute('data-ga-input');
          var input = inputSelector ? document.querySelector(inputSelector) : null;
          if (inputSelector && (!input || !input.value.trim())) return;
          trackButton(form);
          if (input) {
            window.gtag('event', 'view_search_results', {
              search_term: redactSearchTerm(input.value),
              search_location: form.getAttribute('data-ga-location') || 'unknown',
              input_length: input.value.trim().length,
              page_path: location.pathname,
            });
          }
        });
      }
    </script>}

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <!-- 允許 Google 搜尋／Discover 使用大圖預覽；unlisted 頁面則完全禁止索引 -->
    <meta name="robots" content={noindex ? 'noindex, nofollow' : 'max-image-preview:large'} />
    <link rel="icon" type="image/png" sizes="any" href="/favicon.png?v=2" />
    <link rel="shortcut icon" type="image/png" href="/favicon.png?v=2" />
    <link rel="apple-touch-icon" href="/aixwang-icon.png" />
    <link rel="canonical" href={canonical} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:locale" content="zh_TW" />
    <meta property="og:site_name" content={SITE.title} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content={String(ogImageWidth)} />
    <meta property="og:image:height" content={String(ogImageHeight)} />
    <meta property="og:image:alt" content={ogImageAlt} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <link rel="alternate" type="application/rss+xml" title={SITE.title} href="/rss.xml" />
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  </head>
  <!-- 底色維持純色紙感（原型 home-b4-lean.html 的 body 就是 background:var(--paper)）。
       改版前這裡有兩道 radial-gradient 粉色暈染，跟髮絲線列的版面語言互相干擾：
       髮絲線靠的是「線在乾淨紙上」的對比，底下有漸層時線的視覺重量會隨位置浮動。 -->
  <body
    data-pagefind-ignore={noindex ? 'all' : undefined}
    class="min-h-screen bg-paper font-display text-ink antialiased"
  >
    <a class="skip-link" href="#main">跳到主要內容</a>

    <header class="site-header" id="site-header" data-open="false">
      <div class="hd-cap">
        <a
          class="hd-mark"
          href="/"
          aria-label={`回首頁 — ${SITE.title}`}
          data-ga-button="header_logo"
          data-ga-label="Aix Logo"
          data-ga-location="header"
          data-ga-action="home"
        >
          <img class="hd-logo" src="/aixwang-icon.png" width="24" height="24" alt="" aria-hidden="true" />
          <span class="hd-mark-t">Aix</span>
        </a>

        <nav class="mainnav" id="mainnav" aria-label="主要導覽">
          <ul>
            {HEADER_NAV.map((item) => (
              <li>
                <a
                  href={item.href}
                  data-ga-button="header_about"
                  data-ga-label={item.label}
                  data-ga-location="header_nav"
                  data-ga-action="navigate"
                >{item.label}<span class="np-go" aria-hidden="true">→</span></a>
              </li>
            ))}
            {DROPDOWN_NAV.map((menu) => (
              <li class:list={['service-nav-item', { 'service-nav-item--accent': menu.accent }]}>
                <div class:list={['service-nav-shell', { 'service-nav-shell--split': menu.href }]}>
                  {menu.href && (
                    <a
                      class="service-nav-direct"
                      href={menu.href}
                      data-ga-button={`header_${menu.key}`}
                      data-ga-label={menu.label}
                      data-ga-location="header_nav"
                      data-ga-action="navigate"
                    >{menu.label}</a>
                  )}
                  <details class="service-nav nav-dropdown" id={`nav-dropdown-${menu.key}`}>
                    <summary
                      aria-label={menu.href ? `展開${menu.label}分類` : undefined}
                      data-ga-button={`header_${menu.key}_dropdown`}
                      data-ga-label={`${menu.label}下拉`}
                      data-ga-location="header_nav"
                      data-ga-action="toggle_dropdown"
                    >
                      <span class:list={{ 'sr-only': menu.href }}>{menu.label}</span>
                      <span class="service-nav-mark" aria-hidden="true">▾</span>
                    </summary>
                    <div class="service-menu service-menu--featured">
                      <div class="service-menu-head">
                        <span>{menu.label}</span>
                        <small>{menu.subtitle}</small>
                      </div>
                      <div class="service-menu-grid">
                        {menu.previews.map((item, itemIndex) => (
                          <div
                            class:list={['service-preview', {
                              'service-preview--icon': 'navIcon' in item,
                              'service-preview--tooka': 'tookaIcon' in item,
                            }]}
                          >
                            <a
                              class="service-preview-main"
                              href={item.href}
                              target={'external' in item && item.external ? '_blank' : undefined}
                              rel={'external' in item && item.external ? 'noreferrer' : undefined}
                              data-ga-button={`header_${menu.key}_card_${itemIndex + 1}`}
                              data-ga-label={item.title}
                              data-ga-location={`header_${menu.key}_dropdown`}
                              data-ga-action="open_content"
                            >
                              <img src={item.image} alt="" width="1200" height="630" loading="lazy" />
                              <span class="service-preview-copy">
                                <small>{item.eyebrow}</small>
                                <strong>{item.title}</strong>
                                <span>{item.description}</span>
                                {item.cta !== '領取' && (
                                  <b class="service-preview-cta">{item.cta}<span aria-hidden="true">→</span></b>
                                )}
                              </span>
                            </a>
                            {item.cta === '領取' && 'repo' in item && (
                              <span class="service-preview-actions">
                                <button
                                  type="button"
                                  class="service-preview-cta"
                                  data-copy-repo={item.repo}
                                  data-ga-button={`header_resources_claim_${itemIndex + 1}`}
                                  data-ga-label={item.title}
                                  data-ga-location="header_resources_dropdown"
                                  data-ga-action="copy_repo"
                                  aria-label={`複製 ${item.title} Repo 連結`}
                                ><span class="service-preview-cta-label">領取</span><span aria-hidden="true">→</span></button>
                                <em>{CLAIM_INSTALL_HINT}</em>
                              </span>
                            )}
                          </div>
                        ))}
                      </div>
                    </div>
                  </details>
                </div>
              </li>
            ))}
          </ul>
        </nav>

        <div class="hd-tools">
          <button
            class="hd-cue"
            id="nav-cue"
            type="button"
            aria-expanded="false"
            aria-controls="mainnav"
            aria-label="開啟選單"
            data-ga-button="header_mobile_menu"
            data-ga-label="手機導覽選單"
            data-ga-location="header"
            data-ga-action="toggle_menu"
          >
            <span class="hd-cue-b" aria-hidden="true"><i></i><i></i></span>
          </button>
        </div>
      </div>
    </header>

    <a
      class="floating-line"
      href={LINKS.line}
      target="_blank"
      rel="noreferrer"
      aria-label="加 LINE 免費諮詢（開新分頁）"
      data-ga-button="floating_line"
      data-ga-label="LINE 免費諮詢"
      data-ga-location="floating_actions"
      data-ga-action="contact"
    >
      <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" focusable="false">
        <path d="M19.365 9.863c.349 0 .63.285.63.631 0 .345-.281.63-.63.63H17.61v1.125h1.755c.349 0 .63.283.63.63 0 .344-.281.629-.63.629h-2.386c-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63h2.386c.346 0 .627.285.627.63 0 .349-.281.63-.63.63H17.61v1.125h1.755zm-3.855 3.016c0 .27-.174.51-.432.596-.064.021-.133.031-.199.031-.211 0-.391-.09-.51-.25l-2.443-3.317v2.94c0 .344-.279.629-.631.629-.346 0-.626-.285-.626-.629V8.108c0-.27.173-.51.43-.595.06-.023.136-.033.194-.033.195 0 .375.104.495.254l2.462 3.33V8.108c0-.345.282-.63.63-.63.345 0 .63.285.63.63v4.771zm-5.741 0c0 .344-.282.629-.631.629-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63.346 0 .628.285.628.63v4.771zm-2.466.629H4.917c-.345 0-.63-.285-.63-.629V8.108c0-.345.285-.63.63-.63.348 0 .63.285.63.63v4.141h1.756c.348 0 .629.283.629.63 0 .344-.282.629-.629.629M24 10.314C24 4.943 18.615.572 12 .572S0 4.943 0 10.314c0 4.811 4.27 8.842 10.035 9.608.391.082.923.258 1.058.59.12.301.079.766.038 1.08l-.164 1.02c-.045.301-.24 1.186 1.049.645 1.291-.539 6.916-4.078 9.436-6.975C23.176 14.393 24 12.458 24 10.314" />
      </svg>
      <span>LINE</span>
    </a>

    <div class="search-chatbot" id="search-chatbot">
      <button
        class="search-chat-toggle"
        id="search-chat-toggle"
        type="button"
        aria-label="開啟站內搜尋"
        aria-expanded="false"
        aria-controls="search-chat-panel"
        data-ga-button="search_chat_toggle"
        data-ga-label="我有問題"
        data-ga-location="floating_actions"
        data-ga-action="toggle_chat"
      >
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M5.8 4.5h12.4A3.8 3.8 0 0 1 22 8.3v6.1a3.8 3.8 0 0 1-3.8 3.8h-7.1L5 21l1.2-3A3.8 3.8 0 0 1 2 14.4V8.3a3.8 3.8 0 0 1 3.8-3.8Z" />
          <circle cx="8" cy="11.4" r="1.15" />
          <circle cx="12" cy="11.4" r="1.15" />
          <circle cx="16" cy="11.4" r="1.15" />
        </svg>
        <span>我有問題</span>
      </button>

      <section class="search-chat-panel" id="search-chat-panel" role="dialog" aria-label="站內搜尋" hidden>
        <div class="search-chat-bar">
          <span class="search-chat-dots" aria-hidden="true"><i></i><i></i><i></i></span>
          <b>alex@aixwang: search</b>
          <button
            id="search-chat-close"
            type="button"
            aria-label="關閉站內搜尋"
            data-ga-button="search_chat_close"
            data-ga-label="關閉問答視窗"
            data-ga-location="search_chat"
            data-ga-action="close"
          >×</button>
        </div>
        <div class="search-chat-screen">
          <p class="search-chat-boot"><span>AIX SITE SEARCH</span><span>READY</span></p>
          <div class="search-chat-dialog">
            <p>你想搜尋什麼文章或教學？</p>
          </div>
          <div class="search-chat-examples" id="search-examples">
            <button type="button" data-search-example="新手要怎麼學 AI" data-search-query="AI 入門" data-ga-button="search_chat_example_ai_beginner" data-ga-label="新手要怎麼學 AI" data-ga-location="search_chat" data-ga-action="preset_search"><span>❯</span> 新手要怎麼學 AI</button>
            <button type="button" data-search-example="如何安裝 Codex" data-search-query="Codex 安裝" data-ga-button="search_chat_example_codex" data-ga-label="如何安裝 Codex" data-ga-location="search_chat" data-ga-action="preset_search"><span>❯</span> 如何安裝 Codex</button>
            <button type="button" data-search-example="如何申請 API" data-search-query="API 申請" data-ga-button="search_chat_example_api" data-ga-label="如何申請 API" data-ga-location="search_chat" data-ga-action="preset_search"><span>❯</span> 如何申請 API</button>
          </div>
          <div class="search-chat-answer" id="search-chat-answer" hidden>
            <p class="search-chat-command"><span>❯</span><b id="search-chat-query"></b></p>
            <div class="search-chat-response">
              <div class="search-chat-results" id="search-results" aria-live="polite"></div>
            </div>
          </div>
          <a
            class="search-chat-line"
            href={LINKS.line}
            target="_blank"
            rel="noreferrer"
            data-ga-button="search_chat_line"
            data-ga-label="加 LINE 免費諮詢"
            data-ga-location="search_chat"
            data-ga-action="contact"
          >
            <img src={lineLogo.src} width="38" height="38" alt="LINE" />
            <span><small>想直接問我？</small><b>加 LINE 免費諮詢</b></span>
            <i aria-hidden="true">→</i>
          </a>
        </div>
        <form
          class="search-chat-form"
          id="search-chat-form"
          role="search"
          data-ga-submit="search_chat_submit"
          data-ga-label="搜尋站內內容"
          data-ga-location="search_chat"
          data-ga-action="search"
          data-ga-input="#site-search"
        >
          <label class="sr-only" for="site-search">搜尋站內文章、教學或工具</label>
          <span aria-hidden="true">alex@aixwang:~$</span>
          <input id="site-search" type="search" autocomplete="off" placeholder="輸入你的問題…" />
          <button type="submit" aria-label="送出搜尋" title="送出搜尋">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <path d="M12 19V5M6.5 10.5 12 5l5.5 5.5" />
            </svg>
          </button>
        </form>
        <div class="search-chat-status" aria-hidden="true"><span>● READY</span><span>ENTER 送出 · ESC 關閉</span></div>
      </section>
    </div>

    <script is:inline>
      /* 收合選單：獨立的 try 區塊，跟下面的搜尋互不影響。
         關鍵順序——`nav-js` 這個 class 只在「開關確定接好」之後才加到 <html>。
         反過來寫的話，只要下面任何一行拋例外，選單會被 CSS 收起來卻沒有東西能打開它，
         導覽就永久消失了。沒有 JS（或 JS 爆炸）時 .nav-js 不存在，
         導覽就是一排正常可見、可點的連結——連結一律寫在初始 HTML 裡，爬蟲讀得到。 */
      (function () {
        try {
          var header = document.getElementById('site-header');
          var cue = document.getElementById('nav-cue');
          var panel = document.getElementById('mainnav');
          var dropdowns = panel ? panel.querySelectorAll('.nav-dropdown') : [];
          if (!header || !cue || !panel) return;

          // 桌機導覽固定展開，手機則是「下拉面板」（用 max-height）。
          // max-height 必須用 scrollHeight 算，所以只能在 JS 裡設；
          // 換到桌機時要把 inline 的 max-height 清掉，免得殘值把橫排導覽壓扁。
          var mqDesktop = window.matchMedia('(min-width:1024px)');

          function applyHeight(open) {
            if (mqDesktop.matches) {
              panel.style.maxHeight = '';
              return;
            }
            // +28px 留給最後一顆連結的 :focus-visible 外框，不然 overflow:hidden 會切掉它
            panel.style.maxHeight = open ? panel.scrollHeight + 28 + 'px' : '0px';
          }

          // 開啟時把焦點送進第一個連結。原因是 DOM 順序：導覽排在工具列之前
          // （桌機膠囊的視覺順序要求如此），開關按鈕卻在工具列裡。焦點若留在按鈕上，
          // 按 Tab 會直接跳過剛展開的六個連結跑進內文——鍵盤使用者等於打不開選單。
          // 關閉時若焦點還在面板裡，收回按鈕上，不讓焦點掉到 body。
          function setOpen(open, moveFocus) {
            // 桌機版不提供收合；僅手機版的選單開關會改變狀態。
            if (mqDesktop.matches) open = true;
            var focusInside = panel.contains(document.activeElement);
            header.setAttribute('data-open', open ? 'true' : 'false');
            cue.setAttribute('aria-expanded', open ? 'true' : 'false');
            cue.setAttribute('aria-label', open ? '關閉選單' : '開啟選單');
            applyHeight(open);
            if (open && moveFocus) {
              var first = panel.querySelector('a');
              if (first) first.focus();
            } else if (!open && focusInside) {
              cue.focus();
            }
          }

          // 保留尺寸重算給舊版膠囊的漸進增強；目前 Dean 式桌機導覽使用完整容器寬。
          // 寫死的版本（原本 636px，依五個連結算的）在 NAV 加入第六項時被
          // overflow:hidden 直接切掉 44px，而且沒有任何警告。
          // 量的是收合狀態下 nav 的 scrollWidth（此時 CSS 把可視寬壓成 0/384，
          // 但 scrollWidth 仍反映內容的自然寬），加上膠囊其餘元件的固定佔用。
          // 字型載入會改變寬度，所以等 document.fonts.ready 之後再量一次。
          function sizeCapsule() {
            if (!window.matchMedia('(min-width: 1024px)').matches) return;
            var list = panel.querySelector('ul');
            if (!list) return;
            // 搜尋已移到右下角，這裡只保留字標、左右內距與邊框。
            var CHROME = 66;
            var need = Math.ceil(list.scrollWidth) + CHROME;
            header.style.setProperty('--capw-open', need + 'px');
          }
          sizeCapsule();
          if (document.fonts && document.fonts.ready) {
            document.fonts.ready.then(sizeCapsule).catch(function () {});
          }
          window.addEventListener('resize', sizeCapsule);

          // 斷點換邊時重算一次，避免手機展開後轉成桌機、殘留 max-height
          var onMQ = function () {
            sizeCapsule();
            setOpen(mqDesktop.matches, false);
          };
          if (mqDesktop.addEventListener) mqDesktop.addEventListener('change', onMQ);
          else if (mqDesktop.addListener) mqDesktop.addListener(onMQ);
          window.addEventListener('resize', onMQ);

          cue.addEventListener('click', function (e) {
            e.stopPropagation();
            setOpen(cue.getAttribute('aria-expanded') !== 'true', true);
          });

          // Esc 關閉並把焦點還給開關（鍵盤使用者不會迷路）
          document.addEventListener('keydown', function (e) {
            if (mqDesktop.matches) return;
            if (e.key !== 'Escape' && e.keyCode !== 27) return;
            if (cue.getAttribute('aria-expanded') !== 'true') return;
            setOpen(false);
            cue.focus();
          });

          // 點面板外面關閉
          document.addEventListener('click', function (e) {
            if (mqDesktop.matches) return;
            if (cue.getAttribute('aria-expanded') !== 'true') return;
            if (panel.contains(e.target) || cue.contains(e.target)) return;
            setOpen(false);
          });

          // 點面板裡的連結後關閉（同頁錨點時才看得出差別，但成本是零）
          panel.addEventListener('click', function (e) {
            if (mqDesktop.matches) return;
            if (e.target.closest && e.target.closest('a')) setOpen(false);
          });

          // 手機版展開任一子選單後內容高度會增加，必須同步重算外層選單高度，
          // 否則預覽卡片會被原本的 max-height 裁掉。
          Array.prototype.forEach.call(dropdowns, function (dropdown) {
            dropdown.addEventListener('toggle', function () {
              if (mqDesktop.matches || header.getAttribute('data-open') !== 'true') return;
              window.requestAnimationFrame(function () { applyHeight(true); });
            });
          });

          setOpen(mqDesktop.matches, false);
          document.documentElement.classList.add('nav-js'); // 全部接好了才收合
        } catch (err) {
          /* 沒接上就維持展開：一排正常可見、可點的導覽連結 */
        }
      })();

      (function () {
        // 參考 DR.DEAN：桌機游標移入即開啟，移出後稍延遲關閉；
        // 觸控裝置維持 details 原生點擊，鍵盤則以 focus 開啟。
        var dropdowns = Array.prototype.slice.call(document.querySelectorAll('.nav-dropdown'));
        if (!dropdowns.length) return;
        var hoverMenu = window.matchMedia('(min-width:1024px) and (hover:hover) and (pointer:fine)');
        var closeTimers = new WeakMap();

        function closeOthers(current) {
          dropdowns.forEach(function (dropdown) {
            if (dropdown !== current) dropdown.removeAttribute('open');
          });
        }

        dropdowns.forEach(function (dropdown) {
          var summary = dropdown.querySelector('summary');
          // 分離式導覽的文字連結與箭頭是兄弟節點；hover 監聽要掛在整個 shell，
          // 才能做到游標碰到文字就展開，而不是只有碰到小箭頭才有反應。
          var hoverTarget = dropdown.closest('.service-nav-shell') || dropdown;
          hoverTarget.addEventListener('mouseenter', function () {
            if (!hoverMenu.matches) return;
            window.clearTimeout(closeTimers.get(dropdown));
            closeOthers(dropdown);
            dropdown.setAttribute('open', '');
          });
          hoverTarget.addEventListener('mouseleave', function () {
            if (!hoverMenu.matches) return;
            closeTimers.set(dropdown, window.setTimeout(function () {
              dropdown.removeAttribute('open');
            }, 120));
          });
          summary.addEventListener('click', function (event) {
            if (!hoverMenu.matches) return;
            event.preventDefault();
            closeOthers(dropdown);
            dropdown.setAttribute('open', '');
          });
          dropdown.addEventListener('focusin', function () {
            if (!window.matchMedia('(min-width:1024px)').matches) return;
            closeOthers(dropdown);
            dropdown.setAttribute('open', '');
          });
          dropdown.addEventListener('focusout', function () {
            window.setTimeout(function () {
              if (!dropdown.contains(document.activeElement)) dropdown.removeAttribute('open');
            }, 0);
          });
          dropdown.addEventListener('toggle', function () {
            if (dropdown.open) closeOthers(dropdown);
          });
        });

        document.addEventListener('click', function (e) {
          dropdowns.forEach(function (dropdown) {
            if (dropdown.open && !dropdown.contains(e.target)) dropdown.removeAttribute('open');
          });
        });
        document.addEventListener('keydown', function (e) {
          if (e.key !== 'Escape' && e.keyCode !== 27) return;
          var openDropdown = dropdowns.find(function (dropdown) { return dropdown.open; });
          if (!openDropdown) return;
          openDropdown.removeAttribute('open');
          var summary = openDropdown.querySelector('summary');
          if (summary) summary.focus();
        });
      })();

      (function () {
        // 「領取」只複製 Repo 網址；預覽卡片則保留為內容入口。
        // 用 data attribute 共用同一套行為，新增領取項目不需再寫一份 JS。
        var copyButtons = document.querySelectorAll('[data-copy-repo]');
        if (!copyButtons.length) return;

        function legacyCopy(text) {
          var field = document.createElement('textarea');
          field.value = text;
          field.setAttribute('readonly', '');
          field.style.position = 'fixed';
          field.style.opacity = '0';
          document.body.appendChild(field);
          field.select();
          var copied = document.execCommand('copy');
          field.remove();
          return copied ? Promise.resolve() : Promise.reject(new Error('copy failed'));
        }

        Array.prototype.forEach.call(copyButtons, function (button) {
          button.addEventListener('click', function () {
            var repo = button.getAttribute('data-copy-repo');
            var label = button.querySelector('.service-preview-cta-label');
            if (!repo || !label) return;
            var copyPromise = navigator.clipboard && navigator.clipboard.writeText
              ? navigator.clipboard.writeText(repo).catch(function () { return legacyCopy(repo); })
              : legacyCopy(repo);
            copyPromise.then(function () {
              label.textContent = '已複製';
              button.setAttribute('aria-label', '已複製 Repo 連結');
              window.setTimeout(function () {
                label.textContent = '領取';
                button.setAttribute('aria-label', '複製 Repo 連結');
              }, 1800);
            }).catch(function () {
              label.textContent = '複製失敗';
            });
          });
        });
      })();

      (function () {
        // 右下角站內搜尋：只查本站索引，不連接外部服務。
        var container = document.getElementById('search-chatbot');
        var toggle = document.getElementById('search-chat-toggle');
        var panel = document.getElementById('search-chat-panel');
        var closeButton = document.getElementById('search-chat-close');
        var form = document.getElementById('search-chat-form');
        var input = document.getElementById('site-search');
        var box = document.getElementById('search-results');
        var examples = document.getElementById('search-examples');
        var answer = document.getElementById('search-chat-answer');
        var queryEcho = document.getElementById('search-chat-query');
        var pagefind = null;
        var pagefindPromise = null;
        var fallbackIndex = null;
        var searchSequence = 0;
        var activeResult = -1;
        function openSearch() {
          if (!panel) return;
          panel.hidden = false;
          document.body.classList.add('search-chat-open');
          if (toggle) {
            toggle.setAttribute('aria-expanded', 'true');
            toggle.setAttribute('aria-label', '關閉站內搜尋');
          }
          window.setTimeout(function () { input && input.focus(); }, 0);
        }
        function closeSearch() {
          if (panel) panel.hidden = true;
          document.body.classList.remove('search-chat-open');
          if (toggle) {
            toggle.setAttribute('aria-expanded', 'false');
            toggle.setAttribute('aria-label', '開啟站內搜尋');
          }
        }
        function loadFallback() {
          if (fallbackIndex) return Promise.resolve(fallbackIndex);
          return fetch('/search-index.json')
            .then(function (r) { return r.json(); })
            .then(function (d) { fallbackIndex = d; return d; });
        }
        function loadPagefind() {
          if (pagefind) return Promise.resolve(pagefind);
          if (pagefindPromise) return pagefindPromise;
          pagefindPromise = new Function('return import("/pagefind/pagefind.js")')()
            .then(function (module) {
              pagefind = module;
              return pagefind.init().then(function () { return pagefind; });
            })
            .catch(function () {
              pagefind = null;
              pagefindPromise = null;
              return null;
            });
          return pagefindPromise;
        }
        function escapeHtml(value) {
          return String(value || '')
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/"/g, '&quot;');
        }
        function render(list) {
          activeResult = list.length ? 0 : -1;
          if (!list.length) {
            box.innerHTML = '<p class="search-chat-empty">找不到符合的文章，請換個關鍵字再問一次。</p>';
          } else {
            box.innerHTML = list
              .slice(0, 5)
              .map(function (item, index) {
                return '<a href="' + escapeHtml(item.u) + '" data-search-result="' + index + '" data-ga-button="search_chat_result" data-ga-label="' + escapeHtml(item.t) + '" data-ga-location="search_chat" data-ga-action="open_result" class="search-chat-result ' + (index === 0 ? 'is-active' : '') + '">' +
                  '<strong>' + escapeHtml(item.t) + '</strong>' +
                  (item.d ? '<span>' + item.d + '</span>' : '') +
                  '</a>';
              })
              .join('');
          }
        }
        function updateActiveResult() {
          box.querySelectorAll('.search-chat-result').forEach(function (item, index) {
            item.classList.toggle('is-active', index === activeResult);
          });
        }
        function searchFallback(query, sequence) {
          return loadFallback().then(function (items) {
            if (sequence !== searchSequence) return;
            var q = query.toLowerCase();
            render(items
              .filter(function (item) {
                return (item.t + item.d + item.g).toLowerCase().indexOf(q) !== -1;
              })
              .map(function (item) {
                return { u: item.u, t: item.t, d: escapeHtml(item.d) };
              }));
          });
        }
        function executeSearch(query) {
          var sequence = ++searchSequence;
          if (queryEcho) queryEcho.textContent = input && input.value.trim() ? input.value.trim() : query;
          if (examples) examples.hidden = true;
          if (answer) answer.hidden = false;
          box.replaceChildren();
          loadPagefind().then(function (engine) {
            if (!engine) return searchFallback(query, sequence);
            return engine.search(query).then(function (search) {
              return Promise.all(search.results.slice(0, 8).map(function (result) {
                return result.data();
              }));
            }).then(function (results) {
              if (!results || sequence !== searchSequence) return;
              render(results.map(function (result) {
                return {
                  u: result.url,
                  t: result.meta && result.meta.title ? result.meta.title : result.url,
                  d: result.excerpt || '',
                };
              }));
            }).catch(function () {
              return searchFallback(query, sequence);
            });
          });
        }
        function searchFor(query) {
          var q = query.trim().toLowerCase();
          if (!q) {
            searchSequence++;
            if (examples) examples.hidden = false;
            if (answer) answer.hidden = true;
            if (box) box.replaceChildren();
            return;
          }
          executeSearch(q);
        }
        if (toggle) {
          toggle.addEventListener('click', function () {
            var open = toggle.getAttribute('aria-expanded') === 'true';
            if (open) closeSearch();
            else openSearch();
          });
        }
        if (closeButton) {
          closeButton.addEventListener('click', function () {
            closeSearch();
            if (toggle) toggle.focus();
          });
        }
        if (input && box && form) {
          input.addEventListener('focus', loadPagefind);
          input.addEventListener('input', function () {
            searchFor(input.value);
          });
          input.addEventListener('keydown', function (e) {
            if (e.key === 'Escape') {
              closeSearch();
              if (toggle) toggle.focus();
              return;
            }
            var results = box.querySelectorAll('.search-chat-result');
            if (e.key === 'ArrowDown') {
              e.preventDefault();
              if (results.length) {
                activeResult = Math.min(activeResult + 1, results.length - 1);
                updateActiveResult();
                results[activeResult].scrollIntoView({ block: 'nearest' });
              }
              return;
            }
            if (e.key === 'ArrowUp') {
              e.preventDefault();
              if (results.length) {
                activeResult = Math.max(activeResult - 1, 0);
                updateActiveResult();
                results[activeResult].scrollIntoView({ block: 'nearest' });
              }
              return;
            }
          });
          form.addEventListener('submit', function (e) {
            e.preventDefault();
            if (input.value.trim()) searchFor(input.value);
          });
          if (examples) {
            examples.addEventListener('click', function (e) {
              var button = e.target.closest('[data-search-example]');
              if (!button) return;
              input.value = button.getAttribute('data-search-example') || '';
              searchFor(button.getAttribute('data-search-query') || input.value);
              input.focus();
            });
          }
          document.addEventListener('click', function (e) {
            if (panel && !panel.hidden && container && !container.contains(e.target)) closeSearch();
          });
          document.addEventListener('keydown', function (e) {
            if ((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === 'k') {
              e.preventDefault();
              openSearch();
            }
          });
        }
      })();
    </script>

    <!-- 頁首改成 sticky（原本是 fixed）之後，main 不再需要用 padding 補回頁首高度。
         sticky 的另一個好處：沒有 JS 時導覽是一排展開的連結，它會把內容往下推而不是蓋住內容。 -->
    <main id="main" data-pagefind-body>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="ft-wrap">
        <div class="ft-grid">
          <div class="ft-brand">
            <p class="ft-mark">{SITE.title}</p>
            <p class="ft-desc">{SITE.description}</p>
          </div>
          <div>
            <p class="ft-h">網站導覽</p>
            <ul class="ft-list">
              {NAV.map((item) => (
                <li><a href={item.href}>{item.label}</a></li>
              ))}
            </ul>
          </div>
          <!-- 這一欄的後 5 條就是 PERSON_SCHEMA.sameAs 那一組，一律帶 rel="me"：
               它們是身分驗證與 E-E-A-T 訊號，砍掉會少一層可信度來源。 -->
          <div>
            <p class="ft-h">找得到我</p>
            <ul class="ft-social" aria-label="社群與聯絡方式">
              <li><a href={LINKS.line} target="_blank" rel="noreferrer" aria-label="LINE 免費諮詢"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M19.4 9.9c.3 0 .6.3.6.6s-.3.6-.6.6h-1.8v1.1h1.8c.3 0 .6.3.6.6s-.3.6-.6.6H17c-.3 0-.6-.3-.6-.6V8.1c0-.3.3-.6.6-.6h2.4c.3 0 .6.3.6.6s-.3.6-.6.6h-1.8v1.1h1.8zm-3.9 3c0 .3-.2.5-.4.6h-.2c-.2 0-.4-.1-.5-.3l-2.4-3.3v2.9c0 .3-.3.6-.6.6s-.6-.3-.6-.6V8.1c0-.3.2-.5.4-.6h.2c.2 0 .4.1.5.3l2.5 3.3V8.1c0-.3.3-.6.6-.6s.6.3.6.6v4.8zm-5.7 0c0 .3-.3.6-.6.6s-.6-.3-.6-.6V8.1c0-.3.3-.6.6-.6s.6.3.6.6v4.8zm-2.5.6H4.9c-.3 0-.6-.3-.6-.6V8.1c0-.3.3-.6.6-.6s.6.3.6.6v4.1h1.8c.3 0 .6.3.6.6s-.3.7-.6.7M24 10.3C24 4.9 18.6.6 12 .6S0 4.9 0 10.3c0 4.8 4.3 8.8 10 9.6.4.1.9.3 1.1.6.1.3.1.8 0 1.1l-.2 1c0 .3-.2 1.2 1 .6 1.3-.5 6.9-4.1 9.4-7 1.9-2.1 2.7-4 2.7-5.9" /></svg><span class="sr-only">LINE 免費諮詢</span></a></li>
              <li><a href={LINKS.email} aria-label="Email 聯絡"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="2.5" y="5" width="19" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="1.8"/><path d="m3.5 7 8.5 6 8.5-6" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/></svg><span class="sr-only">Email 聯絡</span></a></li>
              <li><a href={LINKS.threads} target="_blank" rel="me noreferrer" aria-label="Threads"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12.2 2.5c-5.2 0-8.7 3.5-8.7 9.4 0 6 3.5 9.6 8.9 9.6 4.8 0 8.1-2.8 8.1-6.8 0-3-1.8-5-4.6-5.6-.5-2.3-2-3.5-4.1-3.5-2.2 0-3.7 1.2-4.1 3.1l2 .5c.2-1.1.9-1.7 2.1-1.7 1.1 0 1.8.6 2.1 1.5h-1.2c-3.4 0-5.4 1.5-5.4 4.1 0 2.3 1.8 3.9 4.4 3.9 2.7 0 4.5-1.6 4.5-4.2v-1.6c1.5.6 2.3 1.8 2.3 3.5 0 2.9-2.3 4.8-6 4.8-4.3 0-6.9-2.8-6.9-7.6 0-4.7 2.5-7.4 6.6-7.4 3.1 0 5.3 1.5 6.2 4.2l1.9-.7c-1.2-3.5-4.1-5.5-8.1-5.5zm1.9 9.8c0 1.8-.8 2.8-2.4 2.8-1.4 0-2.3-.8-2.3-1.9 0-1.4 1.1-2.1 3.3-2.1h1.4v1.2z" /></svg><span class="sr-only">Threads</span></a></li>
              <li><a href={LINKS.instagram} target="_blank" rel="me noreferrer" aria-label="Instagram"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="5" fill="none" stroke="currentColor" stroke-width="1.8"/><circle cx="12" cy="12" r="4" fill="none" stroke="currentColor" stroke-width="1.8"/><circle cx="17.5" cy="6.5" r="1" /></svg><span class="sr-only">Instagram</span></a></li>
              <li><a href={LINKS.youtube} target="_blank" rel="me noreferrer" aria-label="YouTube"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M21.6 7.2a3 3 0 0 0-2.1-2.1C17.6 4.6 12 4.6 12 4.6s-5.6 0-7.5.5a3 3 0 0 0-2.1 2.1C2 9.1 2 12 2 12s0 2.9.4 4.8a3 3 0 0 0 2.1 2.1c1.9.5 7.5.5 7.5.5s5.6 0 7.5-.5a3 3 0 0 0 2.1-2.1C22 14.9 22 12 22 12s0-2.9-.4-4.8z"/><path d="m10 15.5 5-3.5-5-3.5z" fill="var(--color-paper)"/></svg><span class="sr-only">YouTube</span></a></li>
              <li><a href={LINKS.facebookGroup} target="_blank" rel="me noreferrer" aria-label="Facebook 社團"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M14 21v-8h2.8l.4-3H14V8.1c0-.9.3-1.5 1.6-1.5h1.7V3.9c-.3 0-1.3-.1-2.5-.1-2.5 0-4.2 1.5-4.2 4.3V10H7.8v3h2.8v8H14z" /></svg><span class="sr-only">Facebook 社團</span></a></li>
              <li><a href={LINKS.github} target="_blank" rel="me noreferrer" aria-label="GitHub"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2a10 10 0 0 0-3.2 19.5c.5.1.7-.2.7-.5v-1.9c-2.8.6-3.4-1.2-3.4-1.2-.5-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 0 1.6 1.1 1.6 1.1.9 1.6 2.4 1.1 2.9.9.1-.7.4-1.1.7-1.3-2.3-.3-4.6-1.1-4.6-4.9 0-1.1.4-2 1-2.7-.1-.3-.4-1.3.1-2.7 0 0 .8-.3 2.8 1a9.6 9.6 0 0 1 5.1 0c2-1.3 2.8-1 2.8-1 .5 1.4.2 2.4.1 2.7.6.7 1 1.6 1 2.7 0 3.8-2.3 4.6-4.6 4.9.4.3.7 1 .7 2V21c0 .3.2.6.7.5A10 10 0 0 0 12 2z" /></svg><span class="sr-only">GitHub</span></a></li>
            </ul>
          </div>
        </div>
        <div class="ft-bottom">
          <span class="ft-copy">© {new Date().getFullYear()} {SITE.title} — AI 一對一私人教學與公開演講</span>
          <span class="ft-legal">
            <a href="/editorial-policy/">編輯政策</a>
            <a href="/privacy/">隱私權政策</a>
            <a href="/disclaimer/">免責聲明</a>
          </span>
        </div>
      </div>
    </footer>

    <style is:global>
      /* ============================================================
         頁首／頁尾：2026-08 改版第二階段
         設計語言的唯一真實來源是 public/_preview/home-b4-lean.html，
         下面的數值全部照該原型（原型的數值又來自 jisto.tw 的 CSS bundle 實測）。

         這段刻意寫成 is:global 的原生 CSS 而不是 Tailwind utility：
         收合膠囊靠的是 max-width／grid-template-columns 的過渡與 :has()，
         用 utility 拼出來會散在十幾個 class 裡，看不出彼此的關係。

         ⚠ 這段是「無層級（unlayered）」CSS，在串接順序上贏過 Tailwind 的
         @layer utilities。互動元件若要切換顯示，統一搭配 [hidden] 明確控制。
         ============================================================ */
      :root {
        --bar: 68px; /* 頁首列高（jisto 實測值） */
        --ease: cubic-bezier(0.16, 1, 0.3, 1);
      }
      @media (max-width: 767px) {
        :root {
          --bar: 62px;
        }
      }

      .skip-link {
        position: absolute;
        left: -9999px;
        top: 0;
        z-index: 99;
        background: var(--color-ink);
        color: #fff;
        padding: 10px 16px;
        font-size: 14px;
        text-decoration: none;
      }
      .skip-link:focus {
        left: 12px;
        top: 12px;
      }

      /* ---------- 頁首外框 ---------- */
      /* sticky 不是 fixed：沒有 JS 時導覽是一排展開的連結，
         它必須把內容往下推，而不是永久蓋住第一屏。 */
      .site-header {
        position: sticky;
        top: 0;
        z-index: 40;
        background: rgb(247 245 241 / 90%); /* = --color-paper 90% */
        -webkit-backdrop-filter: saturate(140%) blur(10px);
        backdrop-filter: saturate(140%) blur(10px);
        border-bottom: 1px solid var(--color-rule);
      }

      /* LINE 與 AI 搜尋在右下角垂直排列：LINE 在上、Chatbot 在下。 */
      .floating-line {
        position: fixed;
        right: max(20px, env(safe-area-inset-right));
        bottom: calc(max(18px, env(safe-area-inset-bottom)) + 78px);
        z-index: 35;
        display: grid;
        place-items: center;
        width: 52px;
        height: 52px;
        border: 1px solid var(--color-accent-2);
        border-radius: 50%;
        background: var(--color-accent);
        color: #fff;
        text-decoration: none;
        box-shadow: 0 8px 24px rgb(22 22 26 / 18%);
        transition:
          background-color 0.18s var(--ease),
          opacity 0.18s var(--ease),
          transform 0.18s var(--ease),
          visibility 0.18s var(--ease);
      }
      body.search-chat-open .floating-line {
        visibility: hidden;
        opacity: 0;
        pointer-events: none;
        transform: translateY(8px);
      }
      .floating-line svg {
        width: 23px;
        height: 23px;
      }
      .floating-line span {
        position: absolute;
        top: calc(100% + 5px);
        color: var(--color-accent-2);
        font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
        font-size: 9px;
        font-weight: 700;
        letter-spacing: 0.08em;
      }
      @media (max-width: 767px) {
        .floating-line {
          /* 手機改與 chatbot 左右並排，避免垂直堆疊時按鈕與標籤互相遮住。 */
          right: calc(max(12px, env(safe-area-inset-right)) + 62px);
          bottom: max(12px, env(safe-area-inset-bottom));
          width: 46px;
          height: 46px;
        }
        .floating-line svg {
          width: 21px;
          height: 21px;
        }
        .floating-line span {
          display: none;
        }
      }

      /* 一組 DOM 兩種版型，靠 flex-wrap 切換：
         手機（<1024px）wrap，導覽掉到第二列 → 下拉面板
         桌機（≥1024px）nowrap，導覽留在同一列 → 橫向膠囊 */
      .hd-cap {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        width: 100%;
        max-width: 72rem; /* 對齊各頁的 max-w-6xl */
        margin: 0 auto;
        padding: 0 20px;
        column-gap: 12px;
        row-gap: 0;
        min-height: var(--bar);
      }
      @media (min-width: 768px) {
        .hd-cap {
          padding: 0 40px;
        }
      }

      /* ---------- 字標 ---------- */
      .hd-mark {
        order: 1;
        margin-inline-end: auto;
        margin-inline-start: 8px;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        min-height: 44px; /* 觸控目標：字標收成方塊時仍有 44px 高 */
        color: var(--color-ink);
        text-decoration: none;
        white-space: nowrap;
      }
      .hd-logo {
        width: 24px;
        height: 24px;
        flex: none;
      }
      .hd-mark-t {
        font-size: 15px;
        font-weight: 600;
        letter-spacing: -0.01em;
      }

      .hd-tools {
        order: 2;
        display: flex;
        align-items: center;
        gap: 6px;
        /* 42px 圓形裡有 12px 的視覺留白。用負 margin 吃掉它，
           讓圖示的墨跡切齊 .hd-cap 的右緣，而不是按鈕外框切齊——
           不補這 11px，頁首看起來會跟內文差一截、像沒對齊。 */
        margin-inline-end: -11px;
      }

      /* ---------- 收合選單開關 ---------- */
      .hd-cue {
        display: none; /* 沒有 JS 就不出現，導覽維持展開 */
        width: 42px;
        height: 42px;
        padding: 0;
        border: 1px solid var(--color-rule-3);
        border-radius: 999px;
        background: transparent;
        cursor: pointer;
        place-items: center;
        transition:
          border-color 0.2s var(--ease),
          background-color 0.2s var(--ease);
      }
      .nav-js .hd-cue {
        display: grid;
      }
      .hd-cue-b {
        position: relative;
        display: block;
        width: 15px;
        height: 9px;
      }
      .hd-cue-b i {
        position: absolute;
        left: 0;
        display: block;
        width: 15px;
        height: 1.3px;
        background: var(--color-ink);
        transform-origin: 50% 50%;
        transition:
          width 0.28s var(--ease),
          transform 0.28s var(--ease),
          top 0.28s var(--ease);
      }
      .hd-cue-b i:nth-child(1) {
        top: 0;
      }
      .hd-cue-b i:nth-child(2) {
        top: 7.7px;
      }
      .hd-cue[aria-expanded='true'] .hd-cue-b i {
        width: 15px;
        top: 3.85px;
      }
      .hd-cue[aria-expanded='true'] .hd-cue-b i:nth-child(1) {
        transform: rotate(45deg);
      }
      .hd-cue[aria-expanded='true'] .hd-cue-b i:nth-child(2) {
        transform: rotate(-45deg);
      }

      /* ---------- 導覽本體：手機下拉、桌機橫排，同一組 DOM ---------- */
      .mainnav {
        order: 3;
        flex-basis: 100%;
      }
      /* 內距放在 ul 上而且不隨開合改變：這樣 panel.scrollHeight 在任何時候
         量到的都是同一個完整高度。若把內距放在會過渡的 .mainnav 上，
         JS 讀 scrollHeight 的那一瞬間內距還停在起始值，算出來的 max-height
         會短一截，最後一顆連結就被 overflow:hidden 切掉。 */
      .mainnav ul {
        padding: 8px 0 16px;
      }
      .mainnav li + li {
        border-top: 1px solid var(--color-rule);
      }
      .mainnav a {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 16px;
        padding: 15px 2px;
        text-decoration: none;
        font-size: 18px;
        font-weight: 500;
        color: var(--color-ink);
        transition: color 0.18s var(--ease);
      }
      .service-nav-shell--split {
        position: relative;
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto;
        align-items: center;
      }
      .service-nav-shell--split .service-nav {
        display: contents;
      }
      .mainnav .service-nav-direct {
        min-width: 0;
      }
      .service-nav-shell--split .service-nav summary {
        padding-inline: 12px 2px;
      }
      .service-nav-shell--split .service-menu {
        grid-column: 1 / -1;
      }
      .service-nav summary {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 16px;
        padding: 15px 2px;
        color: var(--color-ink);
        font-size: 18px;
        font-weight: 500;
        cursor: pointer;
        list-style: none;
        background: transparent;
        transition:
          color 0.18s var(--ease),
          background-color 0.18s var(--ease);
      }
      .service-nav summary::-webkit-details-marker {
        display: none;
      }
      .service-nav-mark {
        color: currentColor;
        font-size: 16px;
        line-height: 1;
        transition: transform 0.2s var(--ease);
      }
      .service-nav[open] .service-nav-mark {
        transform: rotate(180deg);
      }
      .service-nav-item--accent .service-nav summary {
        padding-inline: 14px;
        background: var(--color-accent);
        color: #fff;
        font-weight: 700;
      }
      .service-nav-item--accent .service-nav-shell--split {
        padding-inline-start: 14px;
        background: var(--color-accent);
      }
      .mainnav .service-nav-item--accent .service-nav-direct,
      .service-nav-item--accent .service-nav-shell--split .service-nav summary {
        color: #fff;
        font-weight: 700;
      }
      .service-nav-item--accent .service-nav-shell--split .service-nav summary {
        padding-inline: 10px 14px;
        background: transparent;
      }
      .service-menu {
        padding: 4px 0 16px;
      }
      .service-menu-head {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 16px;
        padding: 0 2px 10px;
      }
      .service-menu-head > span {
        font-size: 13px;
        font-weight: 700;
        color: var(--color-ink);
      }
      .service-menu-head small {
        color: var(--color-ink-3);
        font-size: 11px;
      }
      .service-menu-grid {
        display: grid;
        gap: 10px;
      }
      .service-menu-links {
        display: grid;
        padding: 0;
      }
      .service-menu-links li + li {
        border-top: 1px solid var(--color-rule);
      }
      .mainnav .service-menu-links a {
        padding: 12px 2px;
        font-size: 14px;
      }
      .service-menu-link-copy {
        display: grid;
        min-width: 0;
      }
      .service-menu-link-copy strong {
        font-weight: 500;
      }
      .mainnav .service-menu-links .service-menu-link--icon {
        display: grid;
        grid-template-columns: 32px minmax(0, 1fr) auto;
        align-items: center;
        gap: 10px;
      }
      .service-menu-link--icon > img {
        display: block;
        width: 32px;
        height: 32px;
        object-fit: contain;
      }
      .service-menu-link-copy small {
        margin-top: 2px;
        color: var(--color-ink-3);
        font-size: 10px;
        line-height: 1.45;
      }
      .mainnav .service-preview {
        position: relative;
        display: grid;
        grid-template-columns: 104px minmax(0, 1fr);
        align-items: stretch;
        column-gap: 12px;
        row-gap: 3px;
        padding: 10px;
        border: 1px solid var(--color-rule-2);
        background: var(--color-paper-2);
        color: var(--color-ink);
      }
      .mainnav .service-preview-main {
        display: contents;
        padding: 0;
        color: inherit;
        white-space: normal;
      }
      .service-preview img {
        display: block;
        grid-column: 1;
        grid-row: 1 / span 2;
        width: 104px;
        height: 100%;
        min-height: 55px;
        border: 1px solid var(--color-rule);
        background: #fff;
        object-fit: cover;
      }
      .service-preview--icon img {
        padding: 5px;
        object-fit: contain;
      }
      .service-preview--tooka img {
        padding: 5px;
      }
      .service-preview-copy {
        display: grid;
        grid-column: 2;
        grid-row: 1;
        min-width: 0;
        gap: 3px;
      }
      .service-preview-copy small {
        color: var(--color-accent);
        font-size: 11px;
        font-weight: 600;
      }
      .service-preview-copy strong {
        font-size: 14px;
        line-height: 1.35;
      }
      .service-preview-copy > span {
        color: var(--color-ink-2);
        font-size: 12px;
        line-height: 1.55;
        overflow-wrap: anywhere;
      }
      .service-preview-cta {
        display: inline-flex;
        width: fit-content;
        min-height: 31px;
        align-items: center;
        gap: 8px;
        margin-top: 7px;
        padding: 5px 10px;
        border: 1px solid var(--color-accent);
        border-radius: 3px;
        color: var(--color-accent);
        font-size: 11px;
        font-weight: 600;
        line-height: 1.2;
      }
      button.service-preview-cta {
        appearance: none;
        background: transparent;
        font-family: inherit;
        cursor: pointer;
      }
      .service-preview > .service-preview-actions {
        display: flex;
        grid-column: 2;
        grid-row: 2;
        align-items: center;
        flex-wrap: wrap;
        gap: 7px 10px;
        color: inherit;
        font-size: inherit;
        line-height: inherit;
      }
      .service-preview-actions em {
        color: var(--color-ink-3);
        font-size: 10px;
        font-style: normal;
        line-height: 1.45;
        overflow-wrap: anywhere;
      }
      .np-go {
        font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
        font-size: 12px;
        color: var(--color-accent);
      }
      /* 收合時要一起加 visibility:hidden。只有 max-height:0 的話，
         看不見的連結仍然可以被 Tab 選到，鍵盤焦點會跑進收起來的面板裡。 */
      .nav-js .mainnav {
        max-height: 0;
        overflow: hidden;
        opacity: 0;
        visibility: hidden;
        transition:
          max-height 0.3s var(--ease),
          opacity 0.3s var(--ease),
          visibility 0s linear 0.3s;
      }
      .nav-js .site-header[data-open='true'] .mainnav {
        opacity: 1;
        visibility: visible;
        border-top: 1px solid var(--color-rule);
        transition:
          max-height 0.3s var(--ease),
          opacity 0.3s var(--ease),
          visibility 0s linear 0s;
      }

      /* ---------- 桌機（≥1024px）：Dean 式完整尺寸橫向導覽 ---------- */
      @media (min-width: 1024px) {
        .nav-js .hd-cue {
          display: none;
        }
        .nav-js .site-header {
          position: sticky;
          top: 0;
          margin: 0;
          width: 100%;
          height: 84px;
          border-width: 0 0 1px;
          border-radius: 0;
          background: rgb(247 245 241 / 96%);
          transition: none;
        }
        .nav-js .hd-cap {
          flex-wrap: nowrap;
          max-width: 72rem;
          height: 100%;
          padding: 0 40px;
          min-height: 0;
          column-gap: 0;
        }
        .nav-js .hd-mark {
          margin-inline-start: 16px;
          margin-inline-end: 0;
        }
        .nav-js .hd-mark-t {
          max-width: none;
          overflow: visible;
          font-size: 18px;
        }
        .nav-js .mainnav {
          order: 2;
          flex-basis: auto;
          margin-inline: auto 16px;
          max-height: none;
          max-width: none;
          padding-block: 0;
          border-top: 0;
          overflow: visible;
          opacity: 1;
          visibility: visible;
          transition: none;
        }
        .nav-js .site-header[data-open='true'] .mainnav {
          max-width: none;
          padding-block: 0;
          border-top: 0;
          overflow: visible;
        }
        .nav-js .mainnav ul {
          display: flex;
          align-items: center;
          gap: 30px;
          /* 上下 6px 不只是留白：.mainnav 要 overflow:hidden 才能做 max-width 收合，
             導覽列高度剛好等於連結高度時，:focus-visible 的外框（offset 3px）
             上下會被裁掉，實測只剩兩條直線。左右 12px 同理，給第一顆連結留位置。 */
          padding: 6px 0;
        }
        .nav-js .mainnav li + li {
          border-top: 0;
        }
        .nav-js .mainnav a {
          padding: 12px 0;
          font-size: 18px;
          white-space: nowrap;
          font-weight: 400;
        }
        .service-nav summary {
          align-items: baseline;
          gap: 4px;
          padding: 12px 0;
          font-size: 18px;
          font-weight: 500;
          white-space: nowrap;
        }
        .service-nav {
          position: relative;
        }
        .service-nav-shell--split {
          grid-template-columns: auto auto;
        }
        .mainnav .service-nav-direct {
          padding-right: 5px;
        }
        .service-nav-shell--split .service-nav summary {
          padding-left: 0;
        }
        .service-nav-item--accent .service-nav summary {
          padding-inline: 18px;
          border-radius: 4px;
          font-weight: 700;
        }
        .service-nav-item--accent .service-nav-shell--split {
          padding-inline-start: 18px;
          border-radius: 4px;
        }
        .nav-js .mainnav .service-nav-item--accent .service-nav-direct {
          padding-right: 5px;
        }
        .service-nav-item--accent .service-nav-shell--split .service-nav summary {
          padding-inline: 0 18px;
          border-radius: 0;
        }
        .service-nav-mark {
          font-size: 13px;
        }
        .service-menu {
          position: fixed;
          top: 84px;
          left: 50%;
          z-index: 60;
          width: min(860px, calc(100vw - 32px));
          padding: 24px;
          border: 1px solid var(--color-rule-2);
          background: rgb(247 245 241 / 98%);
          box-shadow: 0 20px 50px rgb(37 32 27 / 14%);
          transform: translateX(-50%);
          -webkit-backdrop-filter: saturate(140%) blur(14px);
          backdrop-filter: saturate(140%) blur(14px);
        }
        .service-menu--plain {
          position: absolute;
          top: calc(100% + 10px);
          left: 50%;
          width: 300px;
          padding: 14px 18px;
          transform: translateX(-50%);
        }
        .service-menu--plain .service-menu-head {
          display: none;
        }
        .nav-js .mainnav .service-menu--plain .service-menu-links {
          display: grid;
          grid-template-columns: 1fr;
          gap: 0;
          padding: 0;
        }
        .nav-js .mainnav .service-menu--plain .service-menu-links li + li {
          border-top: 1px solid var(--color-rule);
        }
        .nav-js .mainnav .service-menu--plain a {
          justify-content: space-between;
          padding: 13px 4px;
          font-size: 14px;
          white-space: normal;
        }
        .service-menu-head {
          padding: 0 0 14px;
        }
        .service-menu-head > span {
          font-size: 15px;
        }
        .service-menu-head small {
          font-size: 12px;
        }
        .service-menu-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 14px;
        }
        .nav-js .mainnav .service-preview {
          grid-template-columns: 152px minmax(0, 1fr);
          column-gap: 18px;
          padding: 15px;
          white-space: normal;
        }
        .nav-js .mainnav .service-preview-main {
          white-space: normal;
        }
        .service-preview img {
          width: 152px;
          min-height: 80px;
        }
        .service-preview--icon img {
          padding: 8px;
        }
        .service-preview--tooka img {
          padding: 8px;
        }
        .service-preview-copy strong {
          font-size: 16px;
        }
        .service-preview-copy > span {
          font-size: 12.5px;
        }
        .nav-js .mainnav .np-go {
          display: none;
        }
        .nav-js .hd-tools {
          order: 3;
          margin-inline-end: 0;
        }
      }

      /* ---------- 右下角站內搜尋 ---------- */
      .search-chatbot {
        position: fixed;
        right: max(18px, env(safe-area-inset-right));
        bottom: max(18px, env(safe-area-inset-bottom));
        z-index: 80;
        font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
        transition:
          opacity 0.18s var(--ease),
          transform 0.18s var(--ease),
          visibility 0.18s var(--ease);
      }
      .search-chat-toggle {
        display: grid;
        width: 56px;
        height: 56px;
        margin-left: auto;
        place-items: center;
        padding: 0;
        border: 1px solid #4b4643;
        border-radius: 50%;
        background: #1d1b1a;
        color: #f7f3ee;
        box-shadow: 0 12px 32px rgb(22 18 17 / 24%);
        cursor: pointer;
      }
      .search-chat-toggle svg {
        width: 25px;
        height: 25px;
        fill: currentColor;
      }
      .search-chat-toggle svg circle {
        fill: #1d1b1a;
      }
      .search-chat-toggle > span {
        position: absolute;
        top: calc(100% + 3px);
        color: var(--color-accent-2);
        font-size: 8px;
        font-weight: 700;
        letter-spacing: 0.1em;
        white-space: nowrap;
      }
      .search-chat-panel {
        position: absolute;
        right: 0;
        bottom: 72px;
        width: min(400px, calc(100vw - 24px));
        max-height: min(640px, calc(100svh - 104px));
        flex-direction: column;
        overflow: hidden;
        border: 1px solid var(--color-rule-2);
        border-radius: 8px;
        background: var(--color-paper);
        color: var(--color-ink);
        box-shadow: 0 24px 70px rgb(22 18 17 / 28%);
      }
      .search-chat-panel:not([hidden]) {
        display: flex;
      }
      .search-chat-bar {
        display: grid;
        min-height: 40px;
        grid-template-columns: auto minmax(0, 1fr) auto;
        align-items: center;
        gap: 10px;
        padding: 0 10px 0 13px;
        border-bottom: 1px solid var(--color-rule);
        background: var(--color-paper-2);
      }
      .search-chat-dots {
        display: flex;
        gap: 5px;
      }
      .search-chat-dots i {
        width: 7px;
        height: 7px;
        border-radius: 50%;
        background: #a94d59;
      }
      .search-chat-dots i:nth-child(2) { background: #b8924f; }
      .search-chat-dots i:nth-child(3) { background: #5d9975; }
      .search-chat-bar b {
        overflow: hidden;
        color: var(--color-ink-3);
        font-size: 9px;
        font-weight: 400;
        text-overflow: ellipsis;
        white-space: nowrap;
      }
      .search-chat-bar button {
        display: grid;
        width: 32px;
        height: 32px;
        place-items: center;
        padding: 0;
        border: 0;
        background: transparent;
        color: var(--color-ink-3);
        font: 300 22px/1 sans-serif;
        cursor: pointer;
      }
      .search-chat-screen {
        min-height: 0;
        flex: 1;
        overflow-y: auto;
        padding: 18px;
        overscroll-behavior: contain;
      }
      .search-chat-boot {
        display: flex;
        justify-content: space-between;
        gap: 12px;
        margin: 0 0 20px;
        color: var(--color-ink-3);
        font-size: 8px;
        letter-spacing: 0.1em;
      }
      .search-chat-dialog,
      .search-chat-response {
        display: block;
      }
      .search-chat-dialog p {
        margin: 0;
        color: var(--color-ink);
        font-family: var(--font-display);
        font-size: 17px;
        font-weight: 600;
        line-height: 1.5;
      }
      .search-chat-examples {
        display: grid;
        gap: 3px;
        margin: 13px 0 0;
      }
      .search-chat-examples[hidden],
      .search-chat-answer[hidden] {
        display: none;
      }
      .search-chat-examples button {
        min-height: 40px;
        padding: 7px 9px;
        border: 1px solid transparent;
        border-radius: 3px;
        background: transparent;
        color: var(--color-ink-2);
        font: 500 11px/1.5 var(--font-display);
        text-align: left;
        cursor: pointer;
      }
      .search-chat-examples button:first-child {
        border-color: var(--color-rule-2);
        background: var(--color-accent-tint);
        color: var(--color-ink);
      }
      .search-chat-examples button span,
      .search-chat-command > span {
        margin-right: 6px;
        color: var(--color-accent);
      }
      .search-chat-answer {
        margin-top: 18px;
      }
      .search-chat-command {
        margin: 0 0 13px;
        color: var(--color-ink-2);
        font-size: 11px;
        line-height: 1.5;
      }
      .search-chat-command b {
        font-weight: 500;
      }
      .search-chat-results {
        display: grid;
        min-width: 0;
        gap: 4px;
      }
      .search-chat-result {
        display: grid;
        gap: 3px;
        padding: 8px 9px;
        border: 1px solid transparent;
        border-radius: 3px;
        color: var(--color-ink-2);
        text-decoration: none;
      }
      .search-chat-result.is-active {
        border-color: var(--color-rule-2);
        background: var(--color-accent-tint);
      }
      .search-chat-result strong {
        color: var(--color-ink);
        font-family: var(--font-display);
        font-size: 11px;
        font-weight: 600;
        line-height: 1.45;
      }
      .search-chat-result span {
        display: -webkit-box;
        overflow: hidden;
        color: var(--color-ink-3);
        font-family: var(--font-display);
        font-size: 9px;
        line-height: 1.5;
        -webkit-box-orient: vertical;
        -webkit-line-clamp: 2;
      }
      .search-chat-empty {
        margin: 0;
        color: var(--color-ink-3);
        font-family: var(--font-display);
        font-size: 10px;
        line-height: 1.6;
      }
      .search-chat-line {
        display: grid;
        min-height: 58px;
        grid-template-columns: 38px minmax(0, 1fr) auto;
        align-items: center;
        gap: 10px;
        margin-top: 18px;
        padding: 9px 11px;
        border: 1px solid var(--color-rule-2);
        border-radius: 4px;
        background: var(--color-surface);
        color: var(--color-ink);
        text-decoration: none;
      }
      .search-chat-line img {
        display: block;
        width: 38px;
        height: 38px;
        border-radius: 8px;
      }
      .search-chat-line span,
      .search-chat-line small,
      .search-chat-line b {
        display: block;
        min-width: 0;
      }
      .search-chat-line small {
        margin-bottom: 2px;
        color: var(--color-ink-3);
        font-family: var(--font-display);
        font-size: 9px;
      }
      .search-chat-line b {
        color: var(--color-ink);
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 600;
      }
      .search-chat-line > i {
        color: var(--color-accent);
        font-size: 13px;
        font-style: normal;
      }
      .search-chat-form {
        display: grid;
        min-height: 52px;
        grid-template-columns: auto minmax(0, 1fr) auto;
        align-items: center;
        gap: 7px;
        padding: 6px 9px 6px 12px;
        border-top: 1px solid var(--color-rule);
        background: var(--color-paper-2);
      }
      .search-chat-form > span {
        color: var(--color-ink-3);
        font-size: 8px;
      }
      .search-chat-form input {
        width: 100%;
        min-width: 0;
        border: 0;
        outline: 0;
        background: transparent;
        color: var(--color-ink);
        font: 400 11px/1.5 ui-monospace, SFMono-Regular, Menlo, monospace;
      }
      .search-chat-form input::placeholder { color: var(--color-ink-3); }
      .search-chat-form button {
        display: grid;
        width: 34px;
        height: 34px;
        place-items: center;
        padding: 0;
        border: 0;
        border-radius: 50%;
        background: var(--color-accent);
        color: var(--color-paper);
        cursor: pointer;
      }
      .search-chat-form button svg {
        width: 17px;
        height: 17px;
        stroke-width: 1.9;
        stroke-linecap: round;
        stroke-linejoin: round;
      }
      .search-chat-status {
        display: flex;
        min-height: 27px;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        padding: 0 12px;
        border-top: 1px solid var(--color-rule);
        background: var(--color-paper-2);
        color: var(--color-ink-3);
        font-size: 7px;
        letter-spacing: 0.08em;
      }
      .search-chat-status span:first-child { color: var(--color-accent); }
      @media (max-width: 767px) {
        /* 首頁終端機仍在 viewport 時先收起浮動操作，避免遮住搜尋列與狀態列。 */
        body.home-cli-visible:not(.search-chat-open) .floating-line,
        body.home-cli-visible:not(.search-chat-open) .search-chatbot {
          visibility: hidden;
          opacity: 0;
          pointer-events: none;
          transform: translateY(8px);
        }
        .search-chatbot {
          right: max(12px, env(safe-area-inset-right));
          bottom: max(12px, env(safe-area-inset-bottom));
        }
        .search-chat-toggle {
          width: 50px;
          height: 50px;
        }
        .search-chat-toggle > span {
          display: none;
        }
        .search-chat-panel {
          bottom: 64px;
          max-height: calc(100svh - 88px - env(safe-area-inset-bottom));
        }
        .search-chat-screen { padding: 14px 12px; }
        .search-chat-dialog p { font-size: 15px; }
        .search-chat-examples { margin-left: 0; }
        .search-chat-command { margin-left: 0; }
      }

      /* ---------- 頁尾 ---------- */
      .site-footer {
        margin-top: 96px;
        border-top: 1px solid var(--color-rule);
        background: var(--color-paper-2);
        padding: 48px 0 36px;
      }
      .ft-wrap {
        width: 100%;
        max-width: 72rem;
        margin: 0 auto;
        padding: 0 20px;
      }
      @media (min-width: 768px) {
        .ft-wrap {
          padding: 0 40px;
        }
      }
      .ft-grid {
        display: grid;
        gap: 30px;
      }
      @media (min-width: 1024px) {
        .ft-grid {
          grid-template-columns: minmax(300px, 1fr) 190px 268px;
          gap: 24px;
          align-items: start;
        }
      }
      .ft-mark {
        font-size: 15px;
        font-weight: 600;
        color: var(--color-ink);
      }
      .ft-desc {
        margin-top: 12px;
        max-width: 42ch;
        font-size: 13px;
        line-height: 1.85;
        color: var(--color-ink-3);
      }
      .ft-h {
        margin-bottom: 12px;
        font-size: 11.5px;
        letter-spacing: 0.12em;
        color: var(--color-ink-3);
      }
      .ft-list li + li {
        margin-top: 8px;
      }
      .ft-list a {
        font-size: 13.5px;
        color: var(--color-ink-2);
        text-decoration: none;
      }
      @media (min-width: 1024px) {
        .ft-list {
          display: flex;
          flex-wrap: wrap;
          gap: 8px 16px;
          max-width: 190px;
        }
        .ft-list li + li {
          margin-top: 0;
        }
      }
      .ft-social {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        max-width: 176px;
        padding: 0;
        list-style: none;
      }
      @media (min-width: 1024px) {
        .ft-social {
          flex-wrap: nowrap;
          max-width: none;
        }
      }
      .ft-social a {
        display: grid;
        place-items: center;
        width: 36px;
        height: 36px;
        border: 1px solid var(--color-rule-2);
        border-radius: 50%;
        color: var(--color-ink-2);
        text-decoration: none;
        transition:
          color 0.18s var(--ease),
          border-color 0.18s var(--ease),
          background-color 0.18s var(--ease);
      }
      .ft-social svg {
        display: block;
        width: 18px;
        height: 18px;
        fill: currentColor;
      }
      @media (min-width: 1024px) {
        .ft-social a {
          width: 32px;
          height: 32px;
        }
        .ft-social svg {
          width: 16px;
          height: 16px;
        }
      }
      .ft-bottom {
        margin-top: 34px;
        padding-top: 18px;
        border-top: 1px solid var(--color-rule-2);
        display: flex;
        flex-wrap: wrap;
        gap: 8px 20px;
        justify-content: space-between;
        font-size: 12px;
        color: var(--color-ink-3);
      }
      .ft-copy {
        font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
        font-size: 11.5px;
      }
      .ft-legal {
        display: flex;
        flex-wrap: wrap;
        gap: 6px 18px;
      }
      .ft-legal a {
        color: var(--color-ink-3);
        text-decoration: none;
      }

      /* ============================================================
         hover 一律加裝置條件：觸控裝置上 :hover 會「黏住」，
         點過的元件會停在 hover 狀態直到點別的地方。
         :focus-visible 另外寫一份、不設任何條件（見 global.css）。
         ============================================================ */
      @media (hover: hover) and (pointer: fine) {
        .floating-line:hover {
          background: var(--color-accent-2);
          transform: scale(1.06);
        }
        .search-chat-toggle:hover {
          border-color: #bc6472;
          background: #292524;
          transform: translateY(-2px);
        }
        .hd-cue:hover {
          border-color: var(--color-ink);
        }
        /* jisto 的開關 hover：兩條線縮到 10px、旋轉 ±45°，往中線收成小 ✕ */
        .hd-cue:hover .hd-cue-b i {
          width: 10px;
          top: 3.85px;
        }
        .hd-cue:hover .hd-cue-b i:nth-child(1) {
          transform: rotate(45deg);
        }
        .hd-cue:hover .hd-cue-b i:nth-child(2) {
          transform: rotate(-45deg);
        }
        /* 已展開時 hover 維持完整 ✕，不要縮回 10px */
        .hd-cue[aria-expanded='true']:hover .hd-cue-b i {
          width: 15px;
        }
        .mainnav a:hover,
        .hd-mark:hover,
        .ft-list a:hover,
        .ft-legal a:hover {
          color: var(--color-accent);
        }
        .ft-social a:hover {
          color: var(--color-accent);
          border-color: var(--color-accent);
          background: var(--color-accent-tint);
        }
        .search-chat-examples button:hover,
        .search-chat-result:hover {
          border-color: #514b48;
          background: #272322;
          color: #fffaf4;
        }
        .search-chat-line:hover {
          border-color: #72bf8e;
          background: #24362a;
        }
      }

      /* 鍵盤焦點：不靠 hover 也要有等價的視覺回饋（不設裝置條件） */
      .search-chat-toggle:focus-visible,
      .search-chat-bar button:focus-visible,
      .search-chat-examples button:focus-visible,
      .search-chat-result:focus-visible,
      .search-chat-line:focus-visible,
      .search-chat-form button:focus-visible {
        outline: 2px solid #f09aa7;
        outline-offset: 3px;
      }
      .mainnav a:focus-visible {
        color: var(--color-accent);
      }
      .service-nav summary:focus-visible {
        color: var(--color-accent);
        background: var(--color-accent-tint);
        outline: 2px solid var(--color-accent);
        outline-offset: 3px;
      }
      .service-nav-item--accent .service-nav summary:focus-visible {
        color: #fff;
        background: var(--color-accent-2);
      }
      .floating-line:focus-visible {
        outline: 2px solid var(--color-accent);
        outline-offset: 4px;
      }

      @media (prefers-reduced-motion: reduce) {
        html {
          scroll-behavior: auto;
        }
        .site-header,
        .search-chat-toggle,
        .hd-cue,
        .hd-cue-b i,
        .mainnav,
        .hd-mark-t {
          transition: none !important;
        }
        .service-nav-mark {
          transition: none !important;
        }
        .floating-line {
          transition: none !important;
        }
      }
    </style>
  </body>
</html>
