---
// ============================================================
// /resources/ — 免費資源專區（2026-08 改版第三階段一併新建）
// ------------------------------------------------------------
// 為什麼要有這一頁：改版後的首頁四個入口第一個就是「免費資源 → /resources/」，
// Hero 的主 CTA 也指向這裡。沒有這一頁，首頁最主要的兩個行動點都是 404。
//
// 內容從何而來：CMS 文章 free-resources 的內文，改寫成常設頁面的結構。
// 事實（影片、工具、連結、動機說明）跟原文一致，沒有新增也沒有更動；
// 改的只是敘事順序——原本是一篇「我來介紹一下」的文章，現在是一份常設清單。
//
// 為什麼不從 CMS 撈：那篇文章之後要下架，而公開 API 不回傳草稿，
// 撈法會在下架當天讓這一頁沒有內容。頁面之後由站主直接改這個檔維護。
//
// 資料仍然集中在 src/data/：
//   影片 → src/data/home.ts 的 VIDEOS（不在這裡複製一份標題與 embedId）
//   FAQ  → src/data/faqs.ts 的 RESOURCES_FAQS
//
// ⚠ 已知的中間狀態：/resources/ 與 /blog/free-resources/ 會同時存在、內容重複。
//    下架那篇文章與加轉址不在本階段範圍，由 coordinator 收尾。
// ============================================================
import Breadcrumbs from '../components/Breadcrumbs.astro';
import Cta from '../components/Cta.astro';
import FaqList from '../components/FaqList.astro';
import SectionHeading from '../components/SectionHeading.astro';
import { LINKS, SERVICE_SITE, SITE, breadcrumbSchema, pageBannerUrl } from '../consts';
import { RESOURCES_FAQS } from '../data/faqs';
import { VIDEOS } from '../data/home';
import Base from '../layouts/Base.astro';

// 免費資源頁只直接呈現 YouTube 教學影片；Tooka 的示範與完整說明集中在文章，
// 這裡只放文章入口，避免同一份工具內容在兩頁重複維護。
// 之後在 home.ts 加新的 YouTube 教學影片會自動落到「教學影片」，不用改這一頁。
const tutorialVideos = VIDEOS.filter((video) => video.embedId);

// 這裡原本有一段 HyperFrames（站主是該開源專案的協作者）。2026-08-06 移除，
// 理由：這是「免費資源」頁，每一項都應該點得進去用。而站上沒有 HyperFrames 的
// 教學文章或示範影片、repo 裡也沒有它的網址，那一段只能寫「我是協作者」——
// 一個點不了的免費資源，對讀者沒有用。
// 之後若有了對應的文章、影片或可公開的連結，再加回來。

const schema = [
  breadcrumbSchema([{ name: '首頁', item: `${SITE.url}/` }, { name: '免費資源' }]),
  // 免費資源頁輸出與畫面一致的 FAQPage；/faq/ 也會從同一份 RESOURCES_FAQS
  // 彙整顯示，因此之後增修免費資源問題時不需要再手動複製到總整理頁。
  {
    '@type': 'FAQPage',
    '@id': `${SITE.url}/resources/#faq`,
    mainEntity: RESOURCES_FAQS.map((f) => ({
      '@type': 'Question',
      name: f.question,
      acceptedAnswer: { '@type': 'Answer', text: f.answer },
    })),
  },
];
---

<Base
  title="免費資源｜Codex 安裝、AI Skills 與開源工具"
  description="從 Codex 安裝開始，免費取得可直接交給 AI 安裝的 Skills，再學 Threads／Instagram API 與開源工具 Tooka。先自己動手做，卡關再來問。"
  ogImage={pageBannerUrl('resources')}
  ogImageAlt="免費資源：Codex 安裝、AI Skills 與開源工具"
  schema={schema}
>
  <div class="mx-auto max-w-3xl px-6 py-12">
    <Breadcrumbs items={[{ name: '首頁', href: '/' }, { name: '免費資源' }]} />

    <h1 class="t-h1 mt-8 font-semibold text-ink">免費資源</h1>
    <p class="mt-6 text-lg leading-8 text-ink-2">
      不用急著付費。先把 Codex 裝好，再把現成的 Skills 貼給 AI 安裝，就能直接開始使用；想做社群自動化，下面還有 API 教學和開源工具可以跟著操作。自己跑過一次還是卡關，再來<a
        href={`${SERVICE_SITE.url}/course/`}
        class="text-accent underline underline-offset-4">找我一對一</a
      >，把時間花在刀口上。
    </p>

    <section class="mt-12 border-y border-rule py-8" aria-labelledby="tool-quiz-entry-heading">
      <p class="font-mono text-[11.5px] tracking-[0.12em] text-accent">30 秒測驗</p>
      <SectionHeading id="tool-quiz-entry-heading" size="md" class="mt-2">每個需求該用什麼工具？</SectionHeading>
      <p class="mt-4 max-w-2xl leading-8 text-ink-2">
        不管你想做自動發文、社群圖文、剪輯或配音，第一步都先安裝 Codex。到服務站回答 3 題，我會再依每個需求告訴你要搭配什麼工具。
      </p>
      <a
        href={`${SERVICE_SITE.url}/quiz/`}
        class="mt-6 inline-flex min-h-11 items-center justify-center rounded-[3px] border border-accent bg-accent px-6 py-3 font-semibold text-white no-underline transition hover:border-accent-2 hover:bg-accent-2"
        data-ga-button="resources_tool_quiz"
        data-ga-label="開始 30 秒測驗"
        data-ga-location="resources_quiz_entry"
        data-ga-action="open_quiz"
      >開始 30 秒測驗 →</a>
    </section>

    <aside class="mt-8 border-y border-rule py-5" aria-label="AI 新手學習路線">
      <p class="font-mono text-[11.5px] tracking-[0.12em] text-accent">第一次來這裡？</p>
      <p class="mt-2 leading-7 text-ink-2">
        還不確定該先學什麼，可以先讀<a
          href="/blog/how-to-start-learning-ai/"
          class="font-semibold text-accent underline underline-offset-4">零基礎新手的 AI 四步學習路線</a
        >，再回來依照自己的需求挑資源，不必一次全部學完。
      </p>
    </aside>

    <!-- ---------- 第一步：Codex 安裝 ---------- -->
    <SectionHeading size="md" class="mt-14">第一步：先安裝 Codex</SectionHeading>
    <p class="mt-5 leading-8 text-ink-2">
      Codex 是後面安裝 Skills、執行開源工具與處理錯誤的入口。Windows 新手先用 VS Code 官方 Codex 插件完成安裝、登入與第一次對話，不必一開始碰終端機或額外準備 Linux 環境。
    </p>
    <ul class="mt-7">
      <li class="overflow-hidden border border-rule bg-white/40">
        <a href="/blog/codex-windows-wsl-install/" class="group grid h-full items-center no-underline sm:grid-cols-2">
          <img
            src="/images/blog/codex-windows-wsl-install-thumb.webp"
            alt="Codex Windows 新手安裝教學縮圖"
            class="aspect-video w-full object-cover"
            loading="lazy"
            width="640"
            height="360"
          />
          <span class="block p-5">
            <span class="font-mono text-[11.5px] tracking-[0.12em] text-accent">新手先看</span>
            <strong class="t-h3 mt-2 block font-semibold text-ink group-hover:text-accent">VS Code＋Codex 插件版</strong>
            <span class="mt-2 block leading-7 text-ink-2">不用先碰終端機，完成安裝、登入與第一次對話。</span>
          </span>
        </a>
      </li>
    </ul>

    <!-- ---------- 第二步：Skills ---------- -->
    <SectionHeading size="md" class="mt-16">第二步：把 Skills 貼給 AI 安裝</SectionHeading>
    <div class="mt-6 grid items-stretch overflow-hidden border border-rule bg-white/40 sm:grid-cols-[minmax(280px,0.9fr)_minmax(0,1.1fr)]">
      <div class="flex min-w-0 justify-center bg-ink">
        <video
          class="aspect-[9/16] h-auto w-full object-cover"
          src="/videos/grill-me-reel.mp4"
          poster="/images/grill-me-reel-poster.webp"
          controls
          preload="metadata"
          playsinline
          aria-label="grill-me Skill Instagram Reels"
        />
      </div>
      <div class="flex h-full flex-col justify-center p-6 sm:p-7">
        <p class="font-mono text-[11.5px] tracking-[0.12em] text-accent">本週推薦 Skill</p>
        <h3 class="t-h3 mt-2 font-semibold text-ink"><code>grill-me</code>：動工前先讓 AI 把需求問清楚</h3>
        <p class="mt-3 leading-7 text-ink-2">
          你先跟 AI 說想做什麼，它會一路問到細節講清楚，避免做完才發現不是你要的。安裝後在新對話輸入 <code class="rounded bg-paper-2 px-1.5 py-0.5 text-sm text-ink">/grill-me</code> 即可開始。
        </p>
        <ul class="mt-3 grid gap-1.5 text-sm leading-6 text-ink-2">
          <li>— AI 自己查得到的資料，不會丟回來問你</li>
          <li>— 每個問題會附建議答案，可以直接按編號回覆</li>
          <li>— 細節確認完會先請你確認，不會自己直接開工</li>
        </ul>
        <dl class="mt-4 grid grid-cols-4 border-y border-rule">
          <div class="py-3 pr-2">
            <dt class="text-xs text-ink-3">觀看</dt>
            <dd class="mt-1 font-mono text-lg font-semibold text-ink">47,649</dd>
          </div>
          <div class="border-l border-rule px-3 py-3">
            <dt class="text-xs text-ink-3">按讚</dt>
            <dd class="mt-1 font-mono text-lg font-semibold text-ink">950</dd>
          </div>
          <div class="border-l border-rule px-3 py-3">
            <dt class="text-xs text-ink-3">收藏</dt>
            <dd class="mt-1 font-mono text-lg font-semibold text-ink">1,427</dd>
          </div>
          <div class="border-l border-rule py-3 pl-3">
            <dt class="text-xs text-ink-3">分享</dt>
            <dd class="mt-1 font-mono text-lg font-semibold text-ink">594</dd>
          </div>
        </dl>
        <p class="mt-2 text-xs leading-5 text-ink-3">Meta Insights，2026 年 8 月 13 日快照</p>
        <div class="mt-4 flex flex-wrap gap-3">
          <a
            href="https://github.com/mattpocock/skills"
            target="_blank"
            rel="noreferrer"
            class="inline-flex min-h-11 items-center rounded-[3px] border border-rule-2 px-5 py-2.5 font-semibold text-ink no-underline transition hover:border-accent hover:text-accent"
          >查看 Skills 原始碼</a>
          <a
            href="https://www.instagram.com/reel/DbwqIJkydIQ/"
            target="_blank"
            rel="noreferrer"
            class="inline-flex min-h-11 items-center px-1 py-2.5 font-semibold text-accent underline underline-offset-4"
          >在 Instagram 查看</a>
        </div>
        <p class="mt-5 font-semibold text-ink">複製這段，貼給你的 AI：</p>
        <div class="relative mt-3 overflow-hidden rounded-[3px] border border-rule-2 bg-paper-2">
          <pre id="grill-me-install-prompt" class="overflow-x-auto whitespace-pre-wrap px-4 py-4 pr-20 text-sm leading-7 text-ink"><code>請幫我安裝這個 Skills
https://github.com/mattpocock/skills</code></pre>
          <button
            type="button"
            class="absolute right-2 top-2 min-h-9 rounded-[3px] border border-rule-2 bg-white px-3 text-xs font-semibold text-accent"
            data-copy-target="grill-me-install-prompt"
            data-ga-button="resources_copy_grill_me_skill"
            data-ga-label="複製 grill-me 安裝提示"
            data-ga-location="resources_skills"
            data-ga-action="copy_prompt"
            aria-label="複製 grill-me 安裝提示"
          >複製</button>
        </div>
        <p class="mt-3 text-sm leading-6 text-ink-3">貼進 Codex，確認來源後再允許安裝。</p>
      </div>
    </div>

    <!-- ---------- 延伸資源：只有對應需求才需要看 ---------- -->
    <section class="mt-16 border-t border-rule pt-10" aria-labelledby="optional-resources-heading">
      <p class="font-mono text-[11.5px] tracking-[0.12em] text-accent">延伸資源</p>
      <SectionHeading id="optional-resources-heading" size="md" class="mt-2">有相關需求再往下看</SectionHeading>
      <p class="mt-4 leading-8 text-ink-2">前兩步完成後就能開始使用 AI。下面兩項不是必做，也不用照順序；想做社群自動化或社群圖文時再看即可。</p>
    </section>

    <!-- ---------- 選配：API 教學影片 ---------- -->
    <SectionHeading size="md" class="mt-12">想做社群自動化：API 串接</SectionHeading>
    <ul class="mt-6">
      {tutorialVideos.map((video) => (
        <li class="grid gap-5 border-t border-rule px-0.5 py-7 sm:grid-cols-2 sm:gap-8">
          <div class="aspect-video w-full bg-ink">
            <iframe
              class="h-full w-full"
              src={`https://www.youtube-nocookie.com/embed/${video.embedId}`}
              title={video.title}
              loading="lazy"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
              referrerpolicy="strict-origin-when-cross-origin"
              allowfullscreen
            />
          </div>
          <div>
            <!-- 標籤用等寬小標（同 Base 頁尾的 .ft-h 那一階），不做成藥丸標籤：
                 改版後卡片與藥丸標籤全站移除，分類靠字級與字距分出來就夠了。 -->
            <p class="font-mono text-[11.5px] tracking-[0.12em] text-ink-3">{video.tag}</p>
            <h3 class="t-h3 mt-2 font-semibold text-ink">{video.title}</h3>
            <p class="mt-3 leading-8 text-ink-2">{video.description}</p>
          </div>
        </li>
      ))}
    </ul>
    <p class="mt-6 text-sm leading-7 text-ink-2">
      搭配文章：<a href="/blog/threads-api-tutorial/" class="text-accent underline underline-offset-4">Meta 三大 API 申請教學</a>。更多影片在我的<a
        href={LINKS.youtube}
        target="_blank"
        rel="noreferrer"
        class="text-accent underline underline-offset-4">YouTube 頻道</a
      >。
    </p>

    <!-- ---------- 選配：Tooka 文章入口 ---------- -->
    <SectionHeading size="md" class="mt-16">想做社群圖文：Tooka</SectionHeading>
    <a
      href="/blog/why-i-built-tooka/"
      class="group mt-6 grid items-center overflow-hidden border border-rule bg-white/40 no-underline sm:grid-cols-[minmax(0,0.9fr)_minmax(0,1.1fr)]"
    >
      <img
        src="/images/blog/why-i-built-tooka-thumb.webp"
        alt="我為什麼開發開源工具 Tooka 文章縮圖"
        class="aspect-video w-full object-cover"
        loading="lazy"
        width="640"
        height="360"
      />
      <span class="p-6 sm:p-8">
        <span class="font-mono text-[11.5px] tracking-[0.12em] text-accent">開源工具</span>
        <strong class="t-h3 mt-2 block font-semibold text-ink group-hover:text-accent">AI 圖文生成工具怎麼選？我為什麼自己開發 Tooka</strong>
        <span class="mt-4 block leading-8 text-ink-2">從做這個工具的原因、實際功能到使用入口，都整理在完整文章裡。</span>
        <span class="mt-5 block font-semibold text-accent">閱讀 Tooka 完整介紹 →</span>
      </span>
    </a>


    <!-- ---------- FAQ ---------- -->
    <!-- 這三題的 FAQPage schema 在上面的 frontmatter 輸出，理由見那裡的註解：
         原本的宣告來源（CMS 文章 free-resources）已下架，這裡是現在唯一的宣告。
         注意這跟「首頁移除 FAQPage」不衝突：首頁那份是重複宣告，這份是唯一宣告。 -->
    <SectionHeading size="md" class="mt-16">常見問題</SectionHeading>
    <div class="mt-6">
      <FaqList faqs={RESOURCES_FAQS} />
    </div>

    <Cta
      title="有問題想問？"
      text="先完成 Codex 安裝並試用 Skills；API 教學與 Tooka 有需求再看。卡住了再來找我，一對一教學（雙北實體／線上）會直接帶你走一遍。"
      links={[
        { href: LINKS.facebookGroup, label: 'Facebook 社團', external: true },
        { href: `${SERVICE_SITE.url}/course/`, label: '一對一教學', external: true },
      ]}
    />
  </div>

  <script>
    document.querySelectorAll<HTMLButtonElement>('[data-copy-target]').forEach((button) => {
      button.addEventListener('click', async () => {
        const targetId = button.dataset.copyTarget;
        const target = targetId ? document.getElementById(targetId) : null;
        if (!target) return;

        const text = target.textContent?.trim() ?? '';
        try {
          if (navigator.clipboard && window.isSecureContext) {
            await navigator.clipboard.writeText(text);
          } else {
            const textarea = document.createElement('textarea');
            textarea.value = text;
            textarea.style.position = 'fixed';
            textarea.style.opacity = '0';
            document.body.appendChild(textarea);
            textarea.select();
            document.execCommand('copy');
            textarea.remove();
          }
          button.textContent = '已複製 ✓';
          window.setTimeout(() => (button.textContent = '複製'), 1500);
        } catch {
          button.textContent = '請手動複製';
          window.setTimeout(() => (button.textContent = '複製'), 1800);
        }
      });
    });
  </script>
</Base>
