---
// ============================================================
// 首頁（2026-08 改版第三階段）
// ------------------------------------------------------------
// 版面、字級、字距補償、全形問號修剪、捲動揭露的唯一真實來源是
// public/_preview/home-b4-lean.html（變體 B-v4「精簡版」）。
// 這個檔案是把該原型做進 Astro，頁首與頁尾改由 src/layouts/Base.astro 提供。
//
// 改版前是 239 行、七個區塊（免費資源、教學案例、演講經歷、教學內容、
// 精選文章、FAQ、CTA）。原型的結論是：首頁字放到 96px 也救不了資訊過載，
// 所以砍到五個單元 —— 頁首／Hero（含三張服務卡）／信任證明／結尾 CTA／頁尾。
//
// 被移除的區塊都另有去處，沒有內容因此消失：
//   TEACHING_CASES      → /course/、/about/
//   SPEAKING_ENGAGEMENTS→ /services/speaking/、/about/
//   TEACHING_CONTENT    → /course/
//   HOME_FAQS           → /faq/
//   精選文章             → /blog/
//   VIDEOS              → /resources/（本階段一併新建；VIDEOS 原本只有首頁在用）
//
// schema 的兩個處理，方向相反，不可弄錯：
//   Course  保留。它的 teaches 來自 SYLLABUS，而 SYLLABUS 本來就不 render 成畫面、
//           只餵 schema，因此區塊移除不影響它，資料與 schema 都留著。
//   FAQPage 移除。/faq/ 已經有 2 份 FAQPage（HOME_FAQS ＋ 各文章 FAQ），
//           首頁那份從一開始就是重複的。
// ============================================================
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import freeResourcesHome from '../assets/free-resources-home-v2.png';
import instructorShirt from '../assets/instructor-shirt.jpg';
import taichungTalk from '../assets/taichung-talk.jpg';
import tookaCover from '../assets/tooka-cover.jpg';
import { AUTHOR, LINKS, SERVICE_SITE, SITE, pageBannerUrl } from '../consts';
import { SYLLABUS } from '../data/home';
import { TEACHING_CASES } from '../data/services';
import Base from '../layouts/Base.astro';

const HOME_FEATURED_ORDER = ['how-to-start-learning-ai'];
const featuredPosts = (await getCollection('blog', ({ data }) => !data.unlisted && data.featured))
  .sort(
    (a, b) => {
      const aPriority = HOME_FEATURED_ORDER.indexOf(a.id);
      const bPriority = HOME_FEATURED_ORDER.indexOf(b.id);
      if (aPriority !== -1 || bPriority !== -1) {
        if (aPriority === -1) return 1;
        if (bPriority === -1) return -1;
        return aPriority - bPriority;
      }
      return (
        b.data.pubDate.valueOf() - a.data.pubDate.valueOf() ||
        (b.data.cmsId ?? 0) - (a.data.cmsId ?? 0)
      );
    },
  )
  .slice(0, 3);
const schema = [
  {
    '@type': 'Course',
    '@id': `${SITE.url}/#course`,
    name: 'Claude Code/Codex 手把手教學：從 0 到 1 完成第一個作品',
    description:
      '手把手使用 Claude Code 或 Codex，從 0 到 1 完成第一個作品，包含 AI Agent 安裝、實作、測試與除錯。提供雙北實體與線上私人家教。',
    provider: { '@id': AUTHOR.id },
    inLanguage: 'zh-TW',
    courseMode: ['onsite', 'online'],
    teaches: SYLLABUS.map((s) => s.title),
  },
];
---

<Base
  title="Claude Code/Codex 手把手教學｜從 0 到 1 完成第一個作品"
  description={SITE.description}
  ogImage={pageBannerUrl('home')}
  ogImageAlt="AI 新手教學｜從工具到自動化"
  schema={schema}
>
  <div class="home" id="home-root">
    <!-- 1／Hero：對話式介面，讓訪客直接選擇自己目前最接近的狀況 -->
    <section class="hero" aria-labelledby="hero-h">
        <div class="cli hero-reveal" id="hero-cli" tabindex="0" style="--d:0ms">
          <div class="cli-brand" aria-hidden="true">
            <img src="/aixwang-icon.png" width="24" height="24" alt="" />
            <span>Aix</span>
          </div>
          <div class="cli-screen">
            <div class="cli-conversation">
            <div class="cli-dialog">
              <span class="cli-ai-label" aria-hidden="true">AI</span>
              <div class="cli-bubble cli-question-bubble">
                <h1 class="cli-question" id="hero-h" data-cli-question="你希望我怎麼幫助你？">你希望我怎麼幫助你？</h1>
                <span class="cli-caret" aria-hidden="true"></span>
              </div>
            </div>
            <div class="cli-options" aria-label="選擇你需要的幫助">
              <button
                type="button"
                data-answer="手把手帶你從0到1，用 Claude Code 或 Codex，從工具安裝、建立專案規則、環境建置、功能拆分、除錯到完成第一個可用作品，零基礎也能開始。"
                data-image-kind="cases"
                data-href={`${SERVICE_SITE.url}/course/`}
                data-link-label="查看 AI 新手實戰課"
                data-ga-button="hero_cli_newbie"
                data-ga-label="我完全零基礎，想從頭學"
                data-ga-location="hero_cli"
                data-ga-action="select_topic"
              ><span class="cli-option-index" aria-hidden="true">01</span><strong>我完全零基礎，想從頭學</strong><span class="cli-option-arrow" aria-hidden="true">→</span></button>
              <button
                type="button"
                data-answer="曾受 MixerBox、TAIA 與地方社群邀請，分享 AI Agent、社群自動化與對話式拍片，內容以現場實作為主。"
                data-image={taichungTalk.src}
                data-image-alt="Alex 在台中場進行 AI 社群圖文演講"
                data-href={`${SERVICE_SITE.url}/services/speaking/`}
                data-link-label="查看完整演講履歷"
                data-ga-button="hero_cli_speaking"
                data-ga-label="我想找AI講師來演講"
                data-ga-location="hero_cli"
                data-ga-action="select_topic"
              ><span class="cli-option-index" aria-hidden="true">02</span><strong>我想找AI講師來演講</strong><span class="cli-option-arrow" aria-hidden="true">→</span></button>
              <button
                type="button"
                data-answer="推薦從 Tooka 社群圖文工具開始；網站也整理了 Codex、Skills、Meta API 與 AI 自動化的免費資源。"
                data-image={tookaCover.src}
                data-image-alt="Tooka 開源社群圖文卡片工具操作畫面"
                data-href="/resources/"
                data-link-label="查看免費工具與教學"
                data-ga-button="hero_cli_tools"
                data-ga-label="我想先自己摸索免費資源"
                data-ga-location="hero_cli"
                data-ga-action="select_topic"
              ><span class="cli-option-index" aria-hidden="true">03</span><strong>我想先自己摸索免費資源</strong><span class="cli-option-arrow" aria-hidden="true">→</span></button>
              <button
                type="button"
                data-answer="文章依照實作目的整理，可以先從 Codex 工具安裝開始，再依需求查看 Threads 與 Instagram API 申請教學。"
                data-image-kind="articles"
                data-href="/blog/"
                data-link-label="閱讀全部文章"
                data-ga-button="hero_cli_articles"
                data-ga-label="我想找相關主題的文章"
                data-ga-location="hero_cli"
                data-ga-action="select_topic"
              ><span class="cli-option-index" aria-hidden="true">04</span><strong>我想找相關主題的文章</strong><span class="cli-option-arrow" aria-hidden="true">→</span></button>
              <button
                type="button"
                data-answer="無論想做自動發文、社群圖文、剪輯或配音，第一步都先安裝 Codex；回答 3 題後，再告訴你每個需求要搭配哪一個工具。"
                data-image-kind="articles"
                data-href={`${SERVICE_SITE.url}/quiz/`}
                data-link-label="開始 30 秒測驗"
                data-ga-button="hero_cli_quiz"
                data-ga-label="我想知道每個需求該用什麼工具（30秒測驗）"
                data-ga-location="hero_cli"
                data-ga-action="select_topic"
              ><span class="cli-option-index" aria-hidden="true">05</span><strong>我想知道每個需求該用什麼工具（30秒測驗）</strong><span class="cli-option-arrow" aria-hidden="true">→</span></button>
            </div>
            <div class="cli-output" id="cli-output" aria-live="polite" aria-atomic="true">
              <p class="cli-command" id="cli-command-text"></p>
              <p class="cli-thinking" id="cli-thinking" hidden><span aria-hidden="true"></span>AI 正在整理資料…</p>
              <div class="cli-answer" id="cli-answer" hidden>
                <span class="cli-ai-label" aria-hidden="true">AI</span>
                <div class="cli-bubble cli-answer-bubble">
                  <div class="cli-answer-content">
                    <div class="cli-answer-media" id="cli-answer-media">
                      <img id="cli-answer-image" src={taichungTalk.src} alt="" hidden />
                      <div class="cli-case-visual" id="cli-case-visual" aria-label="AI 新手教學實際案例與使用工具">
                        <div class="cli-case-visual-head"><span>CASE LOG</span><b>實際教學案例</b></div>
                        <ul>
                          {TEACHING_CASES.slice(0, 3).map((item) => (
                            <li>
                              <Image src={item.image} alt={item.imageAlt} width={42} />
                              <span><small>{item.student}</small><b>{item.topic}</b></span>
                            </li>
                          ))}
                        </ul>
                      </div>
                      <div class="cli-article-visual" id="cli-article-visual" aria-label="文章分類" hidden>
                        <a href="/blog/codex-windows-wsl-install/" data-ga-button="hero_article_codex" data-ga-label="Codex 安裝教學" data-ga-location="hero_answer" data-ga-action="open_article">
                          <img src="/images/blog/codex-windows-wsl-install.png" alt="Codex Windows 與 WSL 安裝教學" loading="lazy" />
                          <span><small>工具安裝</small><b>Codex 安裝教學</b></span>
                        </a>
                        <div class="cli-article-category">
                          <img src="/images/blog/threads-api-tutorial.png" alt="Threads API 申請教學" loading="lazy" />
                          <span>
                            <small>API 申請</small>
                            <a href="/blog/threads-api-tutorial/" data-ga-button="hero_article_threads_api" data-ga-label="Threads API" data-ga-location="hero_answer" data-ga-action="open_article">Threads API</a>
                            <a href="/blog/instagram-api-tutorial/" data-ga-button="hero_article_instagram_api" data-ga-label="Instagram API" data-ga-location="hero_answer" data-ga-action="open_article">Instagram API</a>
                          </span>
                        </div>
                      </div>
                    </div>
                    <p id="cli-answer-text"></p>
                  </div>
                  <a id="cli-answer-link" href="/" data-ga-button="hero_answer_cta" data-ga-label="繼續查看" data-ga-location="hero_answer" data-ga-action="navigate">繼續查看 <span aria-hidden="true">→</span></a>
                  <button class="cli-end" id="cli-end" type="button" data-ga-button="hero_end_conversation" data-ga-label="結束對話" data-ga-location="hero_answer" data-ga-action="reset"><span aria-hidden="true">×</span> 結束對話</button>
                </div>
              </div>
            </div>
            </div>
          </div>
          <div class="cli-search-area">
            <div class="cli-search-results" id="cli-search-results" aria-live="polite" hidden></div>
            <form class="cli-search-form" id="cli-search-form" role="search" data-ga-submit="hero_search_submit" data-ga-label="搜尋站內內容" data-ga-location="hero_cli" data-ga-action="search" data-ga-input="#cli-search-input">
              <label class="sr-only" for="cli-search-input">搜尋站內文章、教學或工具</label>
              <input id="cli-search-input" type="search" autocomplete="off" placeholder="搜尋文章、教學或工具…" />
              <button type="submit" aria-label="送出搜尋" title="送出搜尋">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
                  <path d="M12 19V5M6.5 10.5 12 5l5.5 5.5" />
                </svg>
              </button>
            </form>
          </div>
        </div>
    </section>

    <div class="home-shell">
      <!-- 2／服務詳細介紹：服務入口的下一層資訊 -->
      <section class="service-details" aria-label="服務詳細介紹">
        <article class="service-row reveal" id="free-resources">
          <div class="service-copy">
            <p class="service-kicker">先自己試試看</p>
            <h2>免費資源</h2>
            <p>先把 Codex 裝好，再把現成的 Skills 交給 AI 安裝。想做社群自動化或社群圖文時，再依需求使用 API 教學與開源工具。</p>
            <ul>
              <li>Codex 新手安裝教學</li>
              <li>可直接交給 AI 安裝的 Skills</li>
              <li>API 串接教學與開源工具</li>
            </ul>
            <a class="service-link" href="/resources/">查看免費資源 <span aria-hidden="true">→</span></a>
          </div>
          <figure class="service-media">
            <Image src={freeResourcesHome} alt="教學影片、開源工具與實作指南組成的免費 AI 學習資源" widths={[560, 880]} sizes="(min-width: 768px) 45vw, 100vw" />
          </figure>
        </article>

        <article class="service-row service-row--reverse reveal">
          <div class="service-copy">
            <p class="service-kicker">實際學員與專案</p>
            <h2>私人家教案例</h2>
            <p>每堂課都用學員自己的電腦、帳號和需求實作。過去已帶學員完成 Agent 安裝、社群自動發文、Email 與行事曆整理，以及 OpenClaw 排錯救援。</p>
            <ul>
              <li>WordPress 串接 Threads 自動發文</li>
              <li>Codex 搜尋 Threads 品牌輿情</li>
              <li>Claude Code 整理 Email、行事曆與 LINE 提醒</li>
            </ul>
            <a class="service-link" href={`${SERVICE_SITE.url}/course/`}>查看完整案例與上課方式 <span aria-hidden="true">→</span></a>
          </div>
          <ul class="case-mini-grid" aria-label="四個私人家教實際案例">
            {TEACHING_CASES.map((item) => (
              <li>
                <Image src={item.image} alt={item.imageAlt} width={64} class="case-mini-icon" />
                <div>
                  <strong>{item.topic}</strong>
                  <span>{item.student}</span>
                </div>
              </li>
            ))}
          </ul>
        </article>

        <article class="service-row reveal">
          <div class="service-copy">
            <p class="service-kicker">現場演示與實作</p>
            <h2>演講紀錄</h2>
            <p>曾於企業、AI 協會與地方社群分享 AI Agent、社群圖文與對話式拍片。內容不只講概念，會把真實工作流搬到現場演示。</p>
            <ul>
              <li>MixerBox：OpenClaw 雙機斷線救援</li>
              <li>TAIA：只靠對話一條龍完成拍片</li>
              <li>台中、雲林：AI 社群圖文實作</li>
            </ul>
            <a class="service-link" href={`${SERVICE_SITE.url}/services/speaking/`}>查看全部演講紀錄與邀約方式 <span aria-hidden="true">→</span></a>
          </div>
          <figure class="service-media">
            <Image src={taichungTalk} alt="Alex 在台中場進行 AI 社群圖文演講" widths={[560, 880]} sizes="(min-width: 768px) 45vw, 100vw" />
          </figure>
        </article>

        <section class="instructor-profile reveal" aria-labelledby="instructor-h">
          <figure class="instructor-photo">
            <Image src={instructorShirt} alt="AI 應用講師 Alex" widths={[420, 700]} sizes="(min-width: 768px) 34vw, 100vw" />
          </figure>
          <div class="instructor-main">
            <p class="service-kicker">從非科班到 AI 應用講師</p>
            <h2 id="instructor-h">關於講師</h2>
            <p class="instructor-name"><span class="lat">Alex Wang</span>／王士麟</p>
            <p class="instructor-intro">接案工程師與資策會講師，也是從沒有技術背景開始，透過 AI 一步一步學會寫程式、做自動化與完成客戶專案，因此特別知道新手容易卡在哪裡。</p>
            <div class="instructor-actions">
              <a class="service-link" href="/about/">了解講師經歷 <span aria-hidden="true">→</span></a>
              <nav class="instructor-socials" aria-label="講師社群連結">
                <a href={LINKS.threads} target="_blank" rel="noreferrer" aria-label="Alex 的 Threads（開新分頁）">
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12.186 24h-.007c-3.581-.024-6.334-1.205-8.184-3.509C2.35 18.44 1.5 15.586 1.472 12.01v-.017c.03-3.579.879-6.43 2.525-8.482C5.845 1.205 8.6.024 12.18 0h.014c2.746.02 5.043.725 6.826 2.098 1.677 1.29 2.858 3.13 3.509 5.467l-2.04.569c-1.104-3.96-3.898-5.984-8.304-6.015-2.91.022-5.11.936-6.54 2.717C4.307 6.504 3.616 8.914 3.589 12c.027 3.086.718 5.496 2.057 7.164 1.43 1.783 3.631 2.698 6.54 2.717 2.623-.02 4.358-.631 5.8-2.045 1.647-1.613 1.618-3.593 1.09-4.798-.31-.71-.873-1.3-1.634-1.75-.192 1.352-.622 2.446-1.284 3.272-.886 1.102-2.14 1.704-3.73 1.79-1.202.065-2.361-.218-3.259-.801-1.063-.689-1.685-1.74-1.752-2.964-.065-1.19.408-2.285 1.33-3.082.88-.76 2.119-1.207 3.583-1.291a13.853 13.853 0 0 1 3.02.142c-.126-.742-.375-1.332-.75-1.757-.513-.586-1.308-.883-2.359-.89h-.029c-.844 0-1.992.232-2.721 1.32L7.734 7.847c.98-1.454 2.568-2.256 4.478-2.256h.044c3.194.02 5.097 1.975 5.287 5.388.108.046.216.094.321.142 1.49.7 2.58 1.761 3.154 3.07.797 1.82.871 4.79-1.548 7.158-1.85 1.81-4.094 2.628-7.277 2.65Zm1.003-11.69c-.242 0-.487.007-.739.021-1.836.103-2.98.946-2.916 2.143.067 1.256 1.452 1.839 2.784 1.767 1.224-.065 2.818-.543 3.086-3.71a10.5 10.5 0 0 0-2.215-.221Z" /></svg>
                </a>
                <a href={LINKS.youtube} target="_blank" rel="noreferrer" aria-label="Alex 的 YouTube（開新分頁）">
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M23.498 6.186a3.003 3.003 0 0 0-2.112-2.123C19.505 3.5 12 3.5 12 3.5s-7.505 0-9.386.563A3.003 3.003 0 0 0 .502 6.186 31.3 31.3 0 0 0 0 12a31.3 31.3 0 0 0 .502 5.814 3.003 3.003 0 0 0 2.112 2.123C4.495 20.5 12 20.5 12 20.5s7.505 0 9.386-.563a3.003 3.003 0 0 0 2.112-2.123A31.3 31.3 0 0 0 24 12a31.3 31.3 0 0 0-.502-5.814ZM9.545 15.568V8.432L15.818 12l-6.273 3.568Z" /></svg>
                </a>
              </nav>
            </div>
          </div>
          <dl class="instructor-facts">
            <div><dt>角色</dt><dd>AI 應用講師<br />全職接案工程師</dd></div>
            <div><dt>專長</dt><dd>AI Agent<br />Vibe Coding<br />工作流自動化</dd></div>
            <div><dt>教學</dt><dd>雙北實體<br />線上私人家教<br />企業與社群演講</dd></div>
          </dl>
        </section>

        {featuredPosts.length > 0 && (
          <section class="featured-posts reveal" aria-labelledby="featured-h">
            <div class="featured-head">
              <div>
                <p class="service-kicker">LATEST ARTICLES</p>
                <h2 id="featured-h">精選文章</h2>
              </div>
              <a href="/blog/">查看全部文章 <span aria-hidden="true">→</span></a>
            </div>
            <ul class="featured-list">
              {featuredPosts.map((post) => (
                <li>
                  <a href={`/blog/${post.id}/`}>
                    <img src={`/images/blog/${post.id}-thumb.webp`} alt="" width="640" height="336" loading="lazy" />
                    <p class="featured-meta"><span>{post.data.tags[0] ?? 'AI 教學'}</span><time datetime={post.data.pubDate.toISOString()}>{post.data.pubDate.toLocaleDateString('zh-TW')}</time></p>
                    <h3>{post.data.title}</h3>
                    <span class="featured-go" aria-hidden="true">閱讀全文 →</span>
                  </a>
                </li>
              ))}
            </ul>
          </section>
        )}
      </section>

      <!-- 4／結尾 CTA -->
      <section class="closer" aria-labelledby="closer-h">
        <h2 class="q q--sec reveal" id="closer-h"><span class="nb">不確定自己適合哪一種<span class="qt">？</span></span></h2>
        <div class="reveal" style="--d:70ms">
          <p class="closer-p">加 <span class="lat">LINE</span> 直接說你想自動化什麼。我會告訴你該走哪一條，也可能直接告訴你哪篇免費文章就能解決。</p>
          <div class="actions">
            <a class="btn btn-line" href={LINKS.line} target="_blank" rel="noreferrer">
              <span class="line-ic" aria-hidden="true"><svg viewBox="0 0 24 24" focusable="false"><path d="M19.365 9.863c.349 0 .63.285.63.631 0 .345-.281.63-.63.63H17.61v1.125h1.755c.349 0 .63.283.63.63 0 .344-.281.629-.63.629h-2.386c-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63h2.386c.346 0 .627.285.627.63 0 .349-.281.63-.63.63H17.61v1.125h1.755zm-3.855 3.016c0 .27-.174.51-.432.596-.064.021-.133.031-.199.031-.211 0-.391-.09-.51-.25l-2.443-3.317v2.94c0 .344-.279.629-.631.629-.346 0-.626-.285-.626-.629V8.108c0-.27.173-.51.43-.595.06-.023.136-.033.194-.033.195 0 .375.104.495.254l2.462 3.33V8.108c0-.345.282-.63.63-.63.345 0 .63.285.63.63v4.771zm-5.741 0c0 .344-.282.629-.631.629-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63.346 0 .628.285.628.63v4.771zm-2.466.629H4.917c-.345 0-.63-.285-.63-.629V8.108c0-.345.285-.63.63-.63.348 0 .63.285.63.63v4.141h1.756c.348 0 .629.283.629.63 0 .344-.282.629-.629.629M24 10.314C24 4.943 18.615.572 12 .572S0 4.943 0 10.314c0 4.811 4.27 8.842 10.035 9.608.391.082.923.258 1.058.59.12.301.079.766.038 1.08l-.164 1.02c-.045.301-.24 1.186 1.049.645 1.291-.539 6.916-4.078 9.436-6.975C23.176 14.393 24 12.458 24 10.314" /></svg></span>
              <span class="btn-l">加 <span class="lat">LINE</span> 免費諮詢</span>
            </a>
          </div>
        </div>
      </section>
    </div>
  </div>

  <script is:inline>
    /* 手機首屏的終端機本身已有站內搜尋；終端機還在畫面時隱藏右下角浮動操作，
       避免 LINE 與 chatbot 蓋住搜尋列／ONLINE 狀態列。滑過終端機後再顯示。 */
    (function () {
      var cli = document.getElementById('hero-cli');
      if (!cli) return;

      function setCliVisible(visible) {
        document.body.classList.toggle('home-cli-visible', visible);
      }

      setCliVisible(true);

      if ('IntersectionObserver' in window) {
        var cliObserver = new IntersectionObserver(function (entries) {
          setCliVisible(entries[0].isIntersecting);
        });
        cliObserver.observe(cli);
        return;
      }

      function updateCliVisibility() {
        var rect = cli.getBoundingClientRect();
        setCliVisible(rect.bottom > 0 && rect.top < window.innerHeight);
      }
      window.addEventListener('scroll', updateCliVisibility, { passive: true });
      window.addEventListener('resize', updateCliVisibility);
      updateCliVisibility();
    })();
  </script>

  <script is:inline>
    /* 逐段浮現：內容 100% 寫在初始 HTML 裡，只有「顯示與否」交給 CSS。
       `js` 這個 class 加在 #home-root（不是 <html>）上，因為這一頁的樣式是
       Astro 的元件範圍樣式——選擇器會被加上 data-astro-cid-*，
       掛在 <html> 上的 class 選不到。
       三層保險，任何一層失效內容都仍然完整可見：
         1. 沒有 JS（或這段拋例外）→ `js` 不會加上 → 所有 .reveal 維持原樣可見
         2. IntersectionObserver 不存在／被停用／行為異常 → 2 秒逾時強制顯示
         3. prefers-reduced-motion → 直接全部顯示，且 CSS 也把過渡關掉 */
    (function () {
      var root = document.getElementById('home-root');
      if (!root) return;
      var reveals = root.querySelectorAll('.reveal, .hero-reveal');

      function revealAll() {
        for (var i = 0; i < reveals.length; i++) reveals[i].classList.add('is-in');
      }

      try {
        root.classList.add('js');

        // 保險 1：先掛逾時，再做任何可能爆炸的事
        var ioAlive = false;
        setTimeout(function () {
          if (!ioAlive) revealAll();
        }, 2000);

        var reduce =
          window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

        // 首屏：立刻揭露（0 / 70 / 140ms 延遲，各 200ms → 總揭露 340ms）
        var hero = root.querySelectorAll('.hero-reveal');
        requestAnimationFrame(function () {
          requestAnimationFrame(function () {
            for (var i = 0; i < hero.length; i++) hero[i].classList.add('is-in');
          });
        });

        if (reduce || !('IntersectionObserver' in window)) {
          revealAll();
          return;
        }

        var io = new IntersectionObserver(
          function (entries) {
            ioAlive = true; // 觀察器確實會回呼 → 逾時保險不必動作
            entries.forEach(function (e) {
              if (e.isIntersecting) {
                e.target.classList.add('is-in');
                io.unobserve(e.target);
              }
            });
          },
          { rootMargin: '0px 0px -8% 0px', threshold: 0.12 },
        );

        var scrollReveals = root.querySelectorAll('.reveal');
        for (var k = 0; k < scrollReveals.length; k++) io.observe(scrollReveals[k]);
      } catch (err) {
        revealAll(); // 保險 2
      }
    })();
  </script>

  <script is:inline>
    (function () {
      var cli = document.getElementById('hero-cli');
      if (!cli) return;
      var question = cli.querySelector('[data-cli-question]');
      var options = Array.prototype.slice.call(cli.querySelectorAll('.cli-options button'));
      var commandText = document.getElementById('cli-command-text');
      var thinking = document.getElementById('cli-thinking');
      var answer = document.getElementById('cli-answer');
      var answerText = document.getElementById('cli-answer-text');
      var answerImage = document.getElementById('cli-answer-image');
      var caseVisual = document.getElementById('cli-case-visual');
      var articleVisual = document.getElementById('cli-article-visual');
      var answerLink = document.getElementById('cli-answer-link');
      var endButton = document.getElementById('cli-end');
      var searchForm = document.getElementById('cli-search-form');
      var searchInput = document.getElementById('cli-search-input');
      var searchResults = document.getElementById('cli-search-results');
      if (!question || !options.length || !commandText || !thinking || !answer || !answerText || !answerImage || !caseVisual || !articleVisual || !answerLink || !endButton || !searchForm || !searchInput || !searchResults) return;

      var timers = [];
      var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      var searchIndexPromise;
      var searchTimer;
      var searchActive = -1;

      function later(callback, delay) {
        var timer = window.setTimeout(callback, delay);
        timers.push(timer);
        return timer;
      }

      function clearResponseTimers() {
        timers.forEach(function (timer) { window.clearTimeout(timer); });
        timers = [];
      }

      function typeText(element, value, speed, done) {
        if (reduce) {
          element.textContent = value;
          if (done) done();
          return;
        }
        element.textContent = '';
        var index = 0;
        function next() {
          element.textContent = value.slice(0, index + 1);
          index += 1;
          if (index < value.length) later(next, speed);
          else if (done) done();
        }
        next();
      }

      function runAnswer(option) {
        if (!cli.classList.contains('is-ready')) return;
        var pageScrollY = window.scrollY;
        clearResponseTimers();
        cli.focus({ preventScroll: true });
        question.textContent = question.dataset.cliQuestion || '';
        cli.classList.add('has-output');
        commandText.textContent = option.querySelector('strong').textContent;
        var showCases = option.dataset.imageKind === 'cases';
        var showArticles = option.dataset.imageKind === 'articles';
        caseVisual.hidden = !showCases;
        articleVisual.hidden = !showArticles;
        answerImage.hidden = showCases || showArticles;
        if (!showCases && !showArticles) {
          answerImage.src = option.dataset.image || '';
          answerImage.alt = option.dataset.imageAlt || '';
        }
        answerLink.href = option.dataset.href || '/';
        answerLink.firstChild.textContent = (option.dataset.linkLabel || '繼續查看') + ' ';
        answerLink.dataset.gaLabel = option.dataset.linkLabel || '繼續查看';

        // 先顯示「AI 正在整理資料…」的思考泡泡，短暫停留後再換成回答泡泡，
        // 讓對話節奏更像真的 AI 聊天產品，而不是內容瞬間切換。
        answer.hidden = true;
        thinking.hidden = false;
        later(function () {
          thinking.hidden = true;
          answer.hidden = false;
          typeText(answerText, option.dataset.answer || '', 14);
        }, reduce ? 0 : 650);

        window.requestAnimationFrame(function () { window.scrollTo(0, pageScrollY); });
      }

      function endConversation() {
        clearResponseTimers();
        cli.classList.remove('has-output');
        thinking.hidden = true;
        answer.hidden = true;
        commandText.textContent = '';
        answerText.textContent = '';
        answerImage.alt = '';
        cli.focus({ preventScroll: true });
      }

      function loadSearchIndex() {
        if (!searchIndexPromise) {
          searchIndexPromise = fetch('/search-index.json')
            .then(function (response) { return response.json(); })
            .catch(function () { return []; });
        }
        return searchIndexPromise;
      }

      function setSearchActive(index) {
        var links = searchResults.querySelectorAll('a');
        if (!links.length) return;
        searchActive = Math.max(0, Math.min(index, links.length - 1));
        links.forEach(function (link, linkIndex) {
          link.classList.toggle('is-active', linkIndex === searchActive);
        });
        links[searchActive].scrollIntoView({ block: 'nearest' });
      }

      function renderSearchResults(items) {
        searchResults.replaceChildren();
        if (!items.length) {
          var empty = document.createElement('p');
          empty.className = 'cli-search-empty';
          empty.textContent = '找不到符合的文章，請嘗試其他關鍵字。';
          searchResults.appendChild(empty);
          searchActive = -1;
        } else {
          items.slice(0, 5).forEach(function (item) {
            var link = document.createElement('a');
            var title = document.createElement('strong');
            var description = document.createElement('span');
            link.className = 'cli-search-result';
            title.className = 'cli-search-title';
            description.className = 'cli-search-description';
            link.href = item.u;
            link.dataset.gaButton = 'hero_search_result';
            link.dataset.gaLabel = item.t;
            link.dataset.gaLocation = 'hero_cli';
            link.dataset.gaAction = 'open_result';
            title.textContent = item.t;
            description.textContent = item.d || '';
            link.append(title, description);
            searchResults.appendChild(link);
          });
          setSearchActive(0);
        }
        searchResults.hidden = false;
      }

      function searchSite(rawQuery) {
        var query = rawQuery.trim().toLowerCase();
        window.clearTimeout(searchTimer);
        if (!query) {
          searchResults.hidden = true;
          searchResults.replaceChildren();
          searchActive = -1;
          return;
        }
        searchResults.hidden = false;
        searchResults.replaceChildren();
        var loading = document.createElement('p');
        loading.className = 'cli-search-empty';
        loading.textContent = '搜尋中…';
        searchResults.appendChild(loading);
        loadSearchIndex().then(function (items) {
          renderSearchResults(items.filter(function (item) {
            return ((item.t || '') + ' ' + (item.d || '') + ' ' + (item.g || ''))
              .toLowerCase()
              .indexOf(query) !== -1;
          }));
        });
      }

      // 每個選項都是獨立按鈕（不再是單選 listbox），點擊直接觸發對應回答，
      // Tab／Enter／Space 走瀏覽器原生按鈕鍵盤行為即可，不需要自訂方向鍵邏輯。
      options.forEach(function (option) {
        option.addEventListener('click', function () { runAnswer(option); });
      });

      endButton.addEventListener('click', endConversation);

      searchInput.addEventListener('focus', loadSearchIndex);
      searchInput.addEventListener('input', function () {
        window.clearTimeout(searchTimer);
        searchTimer = window.setTimeout(function () { searchSite(searchInput.value); }, 180);
      });
      searchInput.addEventListener('keydown', function (event) {
        var links = searchResults.querySelectorAll('a');
        if (event.key === 'ArrowDown' && links.length) {
          event.preventDefault();
          setSearchActive(searchActive + 1);
        } else if (event.key === 'ArrowUp' && links.length) {
          event.preventDefault();
          setSearchActive(searchActive - 1);
        } else if (event.key === 'Enter') {
          event.preventDefault();
          if (links.length && searchActive >= 0) {
            links[searchActive].click();
          } else {
            searchSite(searchInput.value);
          }
        } else if (event.key === 'Escape') {
          searchInput.value = '';
          searchSite('');
        }
      });
      searchForm.addEventListener('submit', function (event) {
        event.preventDefault();
        searchSite(searchInput.value);
      });

      function ready() {
        cli.classList.remove('is-typing');
        cli.classList.add('is-ready');
      }

      if (reduce) {
        ready();
      } else {
        cli.classList.add('is-typing');
        typeText(question, question.dataset.cliQuestion || '', 42, function () { later(ready, 220); });
      }
    })();
  </script>

  <style>
    /* ============================================================
       首頁專屬排版。這裡的每一條都只作用在首頁，不外洩到內頁：
       - 展示級 H1（96px）與襯線體是首頁專用（設計語言定案 #1）
       - 字距分級只套在拉丁字段（.lat）上，中文一律 0
       - 顏色一律用 global.css @theme 的 --color-*，這裡不重新定義任何顏色
       - 內頁字級 .t-h1／.t-h2／.t-h3 這裡完全不碰
       ============================================================ */
    .home {
      /* ---- 字距分級（依光學尺寸，不是一個值套到底）----
         只套在拉丁字段（.lat）上；中文一律 0。
         --ls-display 供 ≥72px 的字段使用。H1 是 clamp(56,8vw,96)，
         8vw 在 viewport 900px 時剛好等於 72px，因此 display 級只在
         min-width:900px 之後才接上；900px 以下 H1 是 56–71.9px，留在 title 級。 */
      --ls-display: -0.06em;
      --ls-title: -0.028em;
      --ls-body: -0.011em;
      --ls-micro: 0.12em;
      --ls: var(--ls-body); /* 拉丁字段預設用內文級 */

      color: var(--color-ink-2);
      font-size: 16px;
      line-height: 1.9;
      letter-spacing: 0; /* 中文：0，不商量 */
    }

    /* 版心對齊 Base 的頁首頁尾（.hd-cap／.ft-wrap 都是 max-width:72rem、
       padding 20/40px）。原型的 .shell 是 1120px，這裡改用 72rem＝1152px，
       為的是 H1 的左緣跟頁首字標、頁尾品牌塊落在同一條垂直線上——
       兩者差 32px 時，捲動經過頁首那條線會看得出來。 */
    .home-shell {
      width: 100%;
      max-width: 72rem;
      margin: 0 auto;
      padding: 0 20px;
    }
    @media (min-width: 768px) {
      .home-shell {
        padding: 0 40px;
      }
    }

    /* ---- 拉丁字段：字距只加在這裡，並補回「尾字距」----
       CSS 的 letter-spacing 會在字段最後一個字元「後面」也加一份間距，
       負值會把後面的中文吸過來、正值會推開。用等量反向的 margin 補回。
       原型是用 .lat 包裹、由 Python 腳本在建置期產生；Astro 沒有那一步，
       首頁內容已砍到 470 字上下，.lat 的位置不多，直接手寫在模板裡。 */
    .lat {
      font-style: normal;
      letter-spacing: var(--ls, 0em);
      margin-inline-end: calc(-1 * var(--ls, 0em));
    }

    /* 12–13px 小標籤：唯一反向加字距的地方（正值，中文也適用），
       同樣要補回尾字距，否則標籤右側會多出一份空白。 */
    .micro {
      letter-spacing: var(--ls-micro);
      margin-inline-end: calc(-1 * var(--ls-micro));
      --ls: 0em; /* 內層 .lat 不再疊加 */
    }

    /* ============================================================
       全形問號的左空白修剪
       ------------------------------------------------------------
       問題：96px 下句末的全形「？」左邊留一個 27px 的空洞。
       實測（Noto Serif TC，upm 1000）：
         U+FF1F  advance 1.000em，墨跡 0.254em → 0.730em
                 左空白 0.254em、右空白 0.270em
         相鄰漢字 始 rsb 0.043em／大 rsb 0.049em／哪 lsb 0.068em
       → 漢字對漢字的墨跡間距約 0.068em，漢字對「？」是 0.297em，差 3.7 倍。
         Chrome 147／96px 實際渲染量測：始？＝28px，始始＝7px。

       ---- 三條「正規解法」全部驗證過，全部不能用 ----
       1. font-feature-settings:"palt" 1
          ／font-variant-east-asian:proportional-width
          Noto Serif TC 的 GPOS 裡「沒有 palt」。108 個子集抽驗 3 個
          （含實際含 U+FF1F 的那個），GPOS 只有 chws/halt/kern/vchw/vhal/vpal。
          halt 的涵蓋範圍只有「」（）！ 等六個字符，chws 只處理成對括號，
          兩者都不含 U+FF1F。實測結果：間距 26px 完全不動。
          同一段 CSS 換成 Noto Sans TC 就有效（27px → 12px）——Sans 有 palt
          且涵蓋 U+FF1F（XPlacement −134／XAdvance −283）。
          也就是說語法沒錯，是這支襯線字型沒有這個功能。
          另外 font-variant-east-asian:proportional-width 對應的是 pwid 而非
          palt，連 Noto Sans TC 都沒有效果（實測 27px 不動）。
       2. text-spacing-trim（CSS Text Level 4）
          Chrome 147 只接受 normal / trim-start / space-all；
          trim-both、trim-all、auto 全部 invalid（CSS.supports 回 false）。
          功能本身確實有在運作：trim-start 會把行首的「切掉左半格
          （墨跡左緣 163px → 115px，正好 0.5em）。
          但三個合法值對「？」的渲染結果一模一樣（26px 不動）——
          規格只裁全形的「開頭／結尾標點」，全形問號、驚嘆號不在裡面。
       3. GSUB 的 hwid／pwid 寬度替代字符
          Noto Serif TC 的 GSUB 只有 ccmp/liga/locl/vert/vrt2，沒有寬度替代。

       ---- 結論與作法 ----
       這支字型沒有任何 OpenType 或 CSS 標準機制可用，只剩光學修剪一條路。
       修剪量不是喬出來的：0.254em（？的左空白）− 0.05em（保留的殘餘空白）
       = 0.204em。留下的 0.05em 讓「？」的墨跡間距落回漢字的節奏上。
       實測驗收（Chrome 147／96px）：始？ 28px → 9px（對照 始始 ＝ 7px）、
                                     大？ 26px → 7px（對照 大大 ＝ 5px）。
       比漢字節奏鬆 2px，這是對的——問號本來就該比字距多留一點點氣。

       副作用與圍堵：
       - 只作用在被 .qt 包起來的那一個字元。頁面其他地方的「，」「。」
         完全不受影響（實測 大，大 加不加 .qt 都是 41/43px）。
       - 修剪量是 em，隨字級等比縮放，所有斷點的光學結果一致，
         不需要為不同字級各補一次。
       - 換行風險：用 .nb（white-space:nowrap）把「開始？」綁成一個
         不可斷的單位，「？」永遠不可能落到行首，也就不會把負 margin
         拉到容器左緣外面。
       - 字型 fallback 風險：若 Noto Serif TC 沒載入，改用 Songti TC 等
         備援字型時修剪量可能不完全匹配。只有在備援字型的「？」左空白
         小於 0.204em 時才會過修——絕大多數中文字型都是 0.24–0.28em，
         風險低，但這條無法在本機驗證，仍屬已知風險。
       ============================================================ */
    .qt {
      margin-inline-start: -0.204em;
    }
    .nb {
      white-space: nowrap;
    }

    /* ---------- Hero：對話式介面（聊天泡泡＋建議選項） ----------
       改版前是深色 macOS 終端機視覺；這裡换成淺色聊天 App 視覺，
       色票一律沿用 global.css @theme 的 --color-*／--paper*，不寫新色碼
       （案例卡、文章卡內部既有的深色卡片色也一併換成同一套亮色語彙）。
       class 命名保留 .cli 前綴：純粹當作這顆元件的樣式命名空間，
       不代表「command line interface」，避免大改名增加不必要的風險。 */
    .hero {
      display: grid;
      width: 100%;
      min-height: calc(100vh - var(--bar));
      min-height: calc(100svh - var(--bar));
      align-items: stretch;
      justify-content: stretch;
      padding: 20px;
    }
    .cli {
      display: flex;
      width: 100%;
      /* 72rem 全站版心扣掉桌機兩側各 40px 內距，與導覽、免費區塊內容左右緣對齊。 */
      max-width: 67rem;
      min-height: calc(100vh - var(--bar) - 40px);
      min-height: calc(100svh - var(--bar) - 40px);
      flex-direction: column;
      margin: 0 auto;
      border: 1px solid var(--color-rule-2);
      border-radius: 14px;
      background: var(--color-paper);
      color: var(--color-ink-2);
      box-shadow: 0 26px 70px rgb(22 22 26 / 10%);
      outline: none;
      overflow: hidden; /* 品牌列／搜尋列的方角背景色塊不會溢出外框的圓角 */
      overflow-anchor: none;
    }
    .cli:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 4px;
    }
    /* 品牌小標記：取代原本的 mac 視窗列，重用頁首同一顆 icon。純裝飾。 */
    .cli-brand {
      display: flex;
      height: 44px;
      flex: none;
      align-items: center;
      gap: 8px;
      padding: 0 18px;
      border-bottom: 1px solid var(--color-rule);
      background: var(--color-paper-2);
      border-radius: 13px 13px 0 0;
    }
    .cli-brand img {
      display: block;
      width: 22px;
      height: 22px;
      border-radius: 5px;
    }
    .cli-brand span {
      color: var(--color-ink-3);
      font-family: var(--font-display);
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.02em;
    }
    .cli-screen {
      display: flex;
      width: 100%;
      min-height: 0;
      flex: 1;
      flex-direction: column;
      padding: clamp(22px, 4vw, 46px);
      font-family: var(--font-display);
    }
    .cli-conversation {
      width: 100%;
    }
    /* 訊息列：AI 頭像＋泡泡。問句與回答共用同一種列。 */
    .cli-dialog {
      display: flex;
      min-width: 0;
      align-items: flex-start;
      gap: 12px;
    }
    .cli-ai-label {
      display: inline-grid;
      width: 30px;
      height: 30px;
      flex: none;
      place-items: center;
      border: 1px solid var(--color-accent-line-2);
      border-radius: 50%;
      background: var(--color-accent-tint);
      color: var(--color-accent);
      font-size: 10px;
      font-weight: 700;
      line-height: 1;
      letter-spacing: 0.04em;
    }
    /* AI 泡泡（提問／回答共用外觀）：卡片邊界＋柔和陰影，靠頭像那一角收窄成 4px，
       模擬對話泡泡的方向指示。 */
    .cli-bubble {
      min-width: 0;
      max-width: 640px;
      padding: 16px 18px;
      border: 1px solid var(--color-rule);
      border-radius: 4px 16px 16px 16px;
      background: var(--color-surface);
      box-shadow: 0 2px 10px rgb(22 22 26 / 6%);
    }
    .cli-question-bubble {
      display: flex;
      align-items: center;
      gap: 4px;
    }
    .cli-question {
      min-width: 0;
      margin: 0;
      color: var(--color-ink);
      font: 600 clamp(20px, 3.2vw, 34px)/1.45 var(--font-display);
      letter-spacing: 0;
      overflow-wrap: anywhere;
    }
    .cli-caret {
      display: inline-block;
      width: 2px;
      height: 1.3em;
      flex: none;
      background: var(--color-accent);
      animation: cli-caret 0.72s step-end infinite;
    }
    .cli.is-ready .cli-caret {
      opacity: 0;
      animation: none;
    }
    /* 建議選項：每個選項各自一張獨立卡片，直向列成一排並排的平行卡片列
       （參考 creatorhome.tw 資源雷達區塊的編號卡片清單），
       不再是擠在一起的膠囊按鈕（見上方 script 的說明）。 */
    .cli-options {
      display: flex;
      flex-direction: column;
      gap: 10px;
      max-width: 720px;
      margin: 6px 0 0 42px;
    }
    .cli-options button {
      display: flex;
      width: 100%;
      min-height: 58px;
      align-items: center;
      gap: 14px;
      padding: 14px 18px;
      border: 1px solid var(--color-rule-2);
      border-radius: 12px;
      background: var(--color-surface);
      color: var(--color-ink-2);
      font: inherit;
      text-align: left;
      cursor: pointer;
      box-shadow: 0 1px 3px rgb(22 22 26 / 6%);
      transition: border-color 0.16s ease, background-color 0.16s ease, color 0.16s ease, box-shadow 0.16s ease, transform 0.16s ease;
    }
    .cli-option-index {
      flex: none;
      color: var(--color-accent);
      font-family: var(--font-display);
      font-size: 13px;
      font-weight: 700;
    }
    .cli-options strong {
      min-width: 0;
      flex: 1;
      color: inherit;
      font-family: var(--font-display);
      font-size: clamp(13px, 1.6vw, 15px);
      font-weight: 600;
      line-height: 1.35;
    }
    .cli-option-arrow {
      flex: none;
      color: var(--color-accent-line-2);
      font-size: 15px;
      transition: transform 0.16s ease;
    }
    .cli-options button:hover .cli-option-arrow {
      transform: translateX(3px);
    }
    .cli.is-typing .cli-options {
      opacity: 0;
      pointer-events: none;
    }
    .cli.is-ready .cli-options {
      animation: cli-options-in 0.34s var(--ease) both;
    }
    .cli.has-output .cli-options {
      display: none;
    }
    .cli-output {
      display: none;
      max-width: 720px;
      margin-top: 20px;
      gap: 16px;
    }
    .cli.has-output .cli-output {
      display: grid;
    }
    /* 使用者選擇泡泡：靠右對齊，酒紅淺色底，像聊天紀錄裡「我剛剛說了什麼」。 */
    .cli-command {
      width: fit-content;
      max-width: 80%;
      margin: 0 0 0 auto;
      padding: 10px 16px;
      border-radius: 16px 4px 16px 16px;
      background: var(--color-accent-tint);
      color: var(--color-ink);
      font-size: 13px;
      font-weight: 500;
      line-height: 1.6;
      box-shadow: 0 2px 8px rgb(122 46 59 / 10%);
    }
    /* AI 思考中：小顆泡泡＋轉圈，取代原本瞬間切換，讓回答泡泡出現前有個緩衝。 */
    .cli-thinking {
      display: flex;
      width: fit-content;
      align-items: center;
      gap: 9px;
      margin-left: 42px;
      padding: 10px 16px;
      border: 1px solid var(--color-rule);
      border-radius: 4px 16px 16px 16px;
      background: var(--color-surface);
      color: var(--color-ink-3);
      font-size: 12px;
    }
    .cli-thinking[hidden],
    .cli-answer[hidden] {
      display: none;
    }
    .cli-thinking > span {
      width: 9px;
      height: 9px;
      border: 1px solid var(--color-rule-3);
      border-top-color: var(--color-accent);
      border-radius: 50%;
      animation: cli-spin 0.75s linear infinite;
    }
    .cli-answer {
      display: flex;
      min-width: 0;
      align-items: flex-start;
      gap: 12px;
      animation: cli-answer-in 0.3s var(--ease) both;
    }
    .cli-answer-bubble {
      display: grid;
      gap: 14px;
    }
    .cli-answer-content {
      display: grid;
      min-width: 0;
      grid-template-columns: minmax(180px, 0.85fr) minmax(220px, 1fr);
      align-items: center;
      gap: clamp(14px, 2.5vw, 24px);
    }
    .cli-answer-media {
      min-width: 0;
    }
    .cli-answer-media > img {
      display: block;
      width: 100%;
      max-height: 230px;
      border: 1px solid var(--color-rule);
      border-radius: 4px;
      background: var(--color-paper-2);
      object-fit: cover;
      aspect-ratio: 16 / 9;
    }
    .cli-answer-media > img[hidden],
    .cli-case-visual[hidden],
    .cli-article-visual[hidden] {
      display: none;
    }
    .cli-case-visual {
      padding: 12px;
      border: 1px solid var(--color-rule);
      border-radius: 4px;
      background: var(--color-paper-2);
    }
    .cli-case-visual-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      padding-bottom: 8px;
      border-bottom: 1px solid var(--color-rule);
    }
    .cli-case-visual-head span {
      color: var(--color-accent);
      font-size: 8px;
      letter-spacing: 0.12em;
    }
    .cli-case-visual-head b {
      color: var(--color-ink);
      font: 600 11px/1.4 var(--font-display);
    }
    .cli-case-visual ul {
      display: grid;
      gap: 6px;
      margin: 8px 0 0;
      padding: 0;
      list-style: none;
    }
    .cli-case-visual li {
      display: grid;
      min-width: 0;
      grid-template-columns: 28px minmax(0, 1fr);
      align-items: center;
      gap: 8px;
      padding: 6px 7px;
      border: 1px solid var(--color-rule);
      border-radius: 3px;
      background: var(--color-surface);
    }
    .cli-case-visual li img {
      width: 28px;
      height: 28px;
      border-radius: 4px;
      object-fit: contain;
    }
    .cli-case-visual li span,
    .cli-case-visual li b,
    .cli-case-visual li small {
      display: block;
      min-width: 0;
    }
    .cli-case-visual li small {
      margin-bottom: 1px;
      color: var(--color-ink-3);
      font-size: 7px;
      line-height: 1.3;
    }
    .cli-case-visual li b {
      overflow: hidden;
      color: var(--color-ink);
      font-size: 8px;
      font-weight: 500;
      line-height: 1.4;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .cli-article-visual {
      display: grid;
      gap: 7px;
    }
    .cli-article-visual > a,
    .cli-article-category {
      display: grid;
      min-width: 0;
      min-height: 72px;
      grid-template-columns: 92px minmax(0, 1fr);
      align-items: center;
      gap: 10px;
      padding: 7px;
      border: 1px solid var(--color-rule);
      border-radius: 4px;
      background: var(--color-paper-2);
      color: var(--color-ink);
      text-decoration: none;
    }
    .cli-article-visual img {
      display: block;
      width: 92px;
      height: 58px;
      border-radius: 3px;
      object-fit: cover;
    }
    .cli-article-visual span,
    .cli-article-visual small,
    .cli-article-visual b {
      display: block;
      min-width: 0;
    }
    .cli-article-visual small {
      margin-bottom: 3px;
      color: var(--color-accent);
      font-size: 8px;
      letter-spacing: 0.08em;
    }
    .cli-article-visual b,
    .cli-article-category span > a {
      color: var(--color-ink);
      font: 500 10px/1.45 var(--font-display);
    }
    .cli-article-category span > a {
      display: inline-block;
      min-height: 32px;
      margin-right: 8px;
      text-decoration: underline;
      text-underline-offset: 3px;
    }
    .cli-answer p {
      margin: 0;
      color: var(--color-ink-2);
      font-family: var(--font-display);
      font-size: clamp(12px, 1.5vw, 14px);
      line-height: 1.9;
    }
    .cli-answer-bubble > a {
      display: inline-flex;
      width: fit-content;
      min-height: 44px;
      align-items: center;
      color: var(--color-accent);
      font-family: var(--font-display);
      font-size: 13px;
      font-weight: 500;
      line-height: 1.5;
      text-underline-offset: 4px;
    }
    .cli-end {
      display: inline-flex;
      width: fit-content;
      min-height: 44px;
      align-items: center;
      padding: 0;
      border: 0;
      background: transparent;
      color: var(--color-ink-3);
      font: 500 12px/1.5 var(--font-display);
      text-decoration: underline;
      text-underline-offset: 4px;
      cursor: pointer;
    }
    .cli-search-area {
      flex: none;
      border-top: 1px solid var(--color-rule);
      background: var(--color-paper-2);
      border-radius: 0 0 13px 13px;
      font-family: var(--font-display);
    }
    .cli-search-results {
      display: grid;
      gap: 3px;
      padding: 12px clamp(15px, 4vw, 46px) 8px;
      border-bottom: 1px solid var(--color-rule);
    }
    .cli-search-results[hidden] { display: none; }
    :global(.cli-search-empty) {
      margin: 0;
      color: var(--color-ink-3);
      font-size: 11px;
      line-height: 1.7;
    }
    :global(.cli-search-result) {
      display: grid;
      gap: 2px;
      padding: 8px 10px;
      border: 1px solid transparent;
      border-radius: 6px;
      color: var(--color-ink-2);
      text-decoration: none;
    }
    :global(.cli-search-result.is-active) {
      border-color: var(--color-accent-line-2);
      background: var(--color-accent-tint);
    }
    :global(.cli-search-title) {
      color: var(--color-ink);
      font-family: var(--font-display);
      font-size: 13px;
      font-weight: 500;
      line-height: 1.5;
    }
    :global(.cli-search-description) {
      overflow: hidden;
      color: var(--color-ink-3);
      font-family: var(--font-display);
      font-size: 10px;
      line-height: 1.5;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .cli-search-form {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      min-height: 56px;
      align-items: center;
      gap: 10px;
      padding: 0 clamp(15px, 4vw, 46px);
    }
    .cli-search-form input {
      min-width: 0;
      padding: 10px 0;
      border: 0;
      outline: 0;
      background: transparent;
      color: var(--color-ink);
      font: 400 14px/1.5 var(--font-display);
    }
    .cli-search-form input::placeholder { color: var(--color-ink-3); }
    .cli-search-form button {
      display: grid;
      width: 36px;
      height: 36px;
      min-height: 36px;
      place-items: center;
      padding: 0;
      border: 1px solid var(--color-accent);
      border-radius: 50%;
      background: var(--color-accent);
      color: var(--color-paper);
      cursor: pointer;
    }
    .cli-search-form button svg {
      width: 17px;
      height: 17px;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    @keyframes cli-caret { 50% { opacity: 0; } }
    @keyframes cli-spin { to { transform: rotate(360deg); } }
    @keyframes cli-options-in {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes cli-answer-in {
      from { opacity: 0; transform: translateY(6px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @media (min-width: 768px) {
      .hero {
        min-height: calc(100vh - 84px);
        min-height: calc(100svh - 84px);
        padding: 32px 40px;
      }
      .cli {
        min-height: calc(100vh - 148px);
        min-height: calc(100svh - 148px);
      }
    }
    @media (max-width: 767px) {
      .cli { border-radius: 10px; }
      .cli-brand {
        height: 36px;
        padding-inline: 12px;
        border-radius: 9px 9px 0 0;
      }
      .cli-brand img { width: 18px; height: 18px; }
      .cli-brand span { font-size: 11px; }
      .cli-screen {
        min-height: 0;
        padding: 18px 12px 22px;
      }
      .cli-dialog {
        gap: 8px;
      }
      .cli-ai-label {
        width: 26px;
        height: 26px;
        font-size: 9px;
      }
      .cli-bubble {
        padding: 12px 14px;
      }
      .cli-question {
        font-size: clamp(17px, 5.2vw, 21px);
      }
      .cli-options {
        gap: 8px;
        margin: 6px 0 0;
      }
      .cli-options button {
        min-height: 48px;
        padding: 12px 14px;
        gap: 10px;
      }
      .cli-options strong { font-size: 13px; }
      .cli-option-index { font-size: 12px; }
      .cli-option-arrow { font-size: 13px; }
      .cli-output {
        margin-top: 16px;
        gap: 12px;
      }
      .cli-command { font-size: 11px; }
      .cli-thinking { margin-left: 0; }
      .cli-answer {
        gap: 8px;
      }
      .cli-answer-content {
        grid-template-columns: 1fr;
        gap: 10px;
      }
      .cli-answer-media > img {
        max-height: 190px;
      }
      .cli-answer p {
        font-size: 11px;
        line-height: 1.75;
      }
      .cli-answer-bubble > a { font-size: 11px; }
      .cli-end { font-size: 10px; }
      .cli-search-results {
        padding-inline: 10px;
      }
      .cli-search-form {
        min-height: 48px;
        gap: 7px;
        padding-inline: 10px;
      }
      .cli-search-form input { font-size: 11px; }
      .cli-search-form button {
        width: 32px;
        height: 32px;
        min-height: 32px;
      }
    }
    @media (max-width: 767px) and (max-height: 520px) {
      .hero { padding-block: 8px; }
      .cli {
        min-height: calc(100vh - var(--bar) - 16px);
        min-height: calc(100svh - var(--bar) - 16px);
      }
      .cli-screen { padding-block: 10px; }
      .cli-conversation { margin-top: 0; }
      .cli-search-form { min-height: 44px; }
    }

    /* 區塊提問只用於頁尾 CTA。 */
    .q {
      color: var(--color-ink);
      font-family: var(--font-serif);
      font-weight: 600;
      letter-spacing: 0;
    }
    .q--sec {
      font-size: clamp(20px, 3vw, 28px);
      line-height: 1.15;
    }
    /* ---------- 按鈕 ---------- */
    .actions {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 26px;
    }
    .hero-contact-actions {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 8px;
      margin-top: 24px;
    }
    .hero-scroll-cue {
      display: flex;
      min-height: 44px;
      align-items: center;
      justify-content: center;
      gap: 8px;
      margin-top: 8px;
      color: var(--color-accent);
      font-size: 12.5px;
      font-weight: 500;
      text-decoration: underline;
      text-underline-offset: 4px;
    }
    .hero-scroll-cue span {
      font-family: var(--font-mono);
      text-decoration: none;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-size: 14.5px;
      font-weight: 500;
      text-decoration: none;
      padding: 12px 22px;
      border-radius: 3px;
      line-height: 1.4;
      /* 44px 觸控目標：12+12 內距 ＋ 14.5×1.4 行框 ≈ 44.3px */
      min-height: 44px;
      transition:
        background-color 0.18s var(--ease),
        border-color 0.18s var(--ease),
        color 0.18s var(--ease),
        transform 0.18s var(--ease);
    }
    .btn-line {
      background: #06c755;
      color: #fff;
      border: 1px solid #06c755;
    }
    .btn-contact {
      border: 1px solid var(--color-rule-3);
      background: rgb(255 255 255 / 34%);
      color: var(--color-ink-2);
    }
    .btn-contact > svg {
      width: 18px;
      height: 18px;
      flex: none;
      stroke-width: 1.8;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    @media (max-width: 767px) {
      .hero-contact-actions {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 5px;
        margin-top: 10px;
      }
      .hero-contact-actions .btn {
        justify-content: center;
        gap: 5px;
        min-width: 0;
        padding: 8px 5px;
        font-size: 11px;
      }
      .hero-contact-actions .line-ic,
      .hero-contact-actions .line-ic svg,
      .hero-contact-actions .btn-contact > svg {
        width: 16px;
        height: 16px;
      }
    }
    .line-ic,
    .line-ic svg {
      display: block;
      width: 20px;
      height: 20px;
    }
    .line-ic svg {
      fill: currentColor;
    }
    /* 按鈕文字必須是「一個」flex 子項。若讓文字節點直接當子項，
       包裹拉丁字段用的 .lat 會被 blockify 成 flex item，gap:8px 就會
       把「加 LINE 免費諮詢」拆成三塊。 */
    .btn-l {
      display: inline;
    }
    .btn .arw {
      font-family: var(--font-mono);
      font-size: 12.5px;
      opacity: 0.85;
      --ls: 0em;
    }

    /* ---------- 首屏下方的服務詳細介紹 ---------- */
    .service-details {
      padding-top: clamp(24px, 4vw, 48px);
    }
    #free-resources {
      scroll-margin-top: calc(var(--bar) + 16px);
    }
    .service-row {
      display: grid;
      gap: 30px;
      padding: clamp(48px, 7vw, 88px) 0;
      border-top: 1px solid var(--color-rule);
    }
    @media (max-width: 767px) {
      .service-row:first-child {
        padding-top: 36px;
      }
    }
    @media (min-width: 768px) {
      .service-row {
        grid-template-columns: minmax(0, 1fr) minmax(280px, 0.88fr);
        gap: clamp(48px, 7vw, 96px);
        align-items: center;
      }
      .service-row--reverse .service-copy {
        order: 2;
      }
      .service-row--reverse .service-media {
        order: 1;
      }
    }
    .service-copy {
      max-width: 58ch;
    }
    .service-kicker {
      margin-bottom: 12px;
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.1em;
      color: var(--color-accent);
    }
    .service-copy h2 {
      font-family: var(--font-serif);
      font-size: clamp(32px, 4vw, 52px);
      line-height: 1.1;
      font-weight: 600;
      color: var(--color-ink);
    }
    .service-copy > p:not(.service-kicker) {
      margin-top: 20px;
      font-size: 16px;
      line-height: 1.9;
      color: var(--color-ink-2);
    }
    .service-copy ul {
      display: grid;
      gap: 8px;
      margin-top: 20px;
      padding: 0;
      list-style: none;
      color: var(--color-ink-3);
      font-size: 14px;
      line-height: 1.65;
    }
    .service-copy li {
      position: relative;
      padding-left: 16px;
    }
    .service-copy li::before {
      content: '—';
      position: absolute;
      left: 0;
      color: var(--color-accent);
    }
    .service-link {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      min-height: 44px;
      margin-top: 24px;
      color: var(--color-accent);
      font-size: 14.5px;
      font-weight: 500;
      text-decoration: underline;
      text-underline-offset: 5px;
    }
    .service-media {
      margin: 0;
      overflow: hidden;
      border: 1px solid var(--color-rule-2);
      background: var(--color-surface);
      aspect-ratio: 4 / 3;
    }
    .service-media img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .service-media--portrait img {
      object-position: center 28%;
    }
    .case-mini-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
      margin: 0;
      padding: 0;
      list-style: none;
    }
    .case-mini-grid li {
      display: grid;
      grid-template-columns: 38px minmax(0, 1fr);
      gap: 11px;
      align-items: start;
      min-height: 126px;
      padding: 14px;
      border: 1px solid var(--color-rule-2);
      background: rgb(255 255 255 / 42%);
    }
    .case-mini-icon {
      width: 38px;
      height: 38px;
      object-fit: contain;
    }
    .case-mini-grid strong,
    .case-mini-grid span {
      display: block;
    }
    .case-mini-grid strong {
      font-size: 13px;
      line-height: 1.55;
      font-weight: 600;
      color: var(--color-ink);
    }
    .case-mini-grid span {
      margin-top: 7px;
      font-size: 11.5px;
      line-height: 1.5;
      color: var(--color-accent);
    }

    /* ---------- 關於講師：人物檔案式資訊層級 ---------- */
    .instructor-profile {
      display: grid;
      grid-template-columns: minmax(220px, 0.72fr) minmax(0, 1.28fr) minmax(180px, 0.62fr);
      gap: clamp(28px, 4vw, 56px);
      align-items: start;
      padding: clamp(56px, 8vw, 96px) 0;
      border-top: 1px solid var(--color-rule);
    }
    .instructor-photo {
      margin: 0;
      overflow: hidden;
      border: 1px solid var(--color-rule-2);
      aspect-ratio: 4 / 5;
      background: var(--color-surface);
    }
    .instructor-photo img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center 28%;
    }
    .instructor-main h2,
    .featured-head h2 {
      font-family: var(--font-serif);
      font-size: clamp(32px, 4vw, 52px);
      line-height: 1.1;
      font-weight: 600;
      color: var(--color-ink);
    }
    .instructor-name {
      margin-top: 18px;
      font-size: 14px;
      font-weight: 600;
      color: var(--color-accent);
    }
    .instructor-intro {
      max-width: 50ch;
      margin-top: 16px;
      font-size: 15px;
      line-height: 1.9;
      color: var(--color-ink-2);
    }
    .instructor-actions {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 14px 22px;
      margin-top: 20px;
    }
    .instructor-actions .service-link {
      margin-top: 0;
    }
    .instructor-socials {
      display: flex;
      gap: 8px;
    }
    .instructor-socials a {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      min-height: 44px;
      padding: 0;
      border: 1px solid var(--color-rule-3);
      border-radius: 999px;
      color: var(--color-ink-2);
      text-decoration: none;
      transition:
        color 0.18s var(--ease),
        border-color 0.18s var(--ease),
        background-color 0.18s var(--ease);
    }
    .instructor-socials svg {
      width: 17px;
      height: 17px;
    }
    .instructor-facts {
      margin: 0;
      border-top: 1px solid var(--color-rule-2);
    }
    .instructor-facts div {
      padding: 14px 0 16px;
      border-bottom: 1px solid var(--color-rule);
    }
    .instructor-facts dt {
      margin-bottom: 6px;
      font-size: 10.5px;
      font-weight: 600;
      letter-spacing: 0.12em;
      color: var(--color-accent);
    }
    .instructor-facts dd {
      margin: 0;
      font-size: 12.5px;
      line-height: 1.65;
      color: var(--color-ink-2);
    }

    /* ---------- 關於講師下方：三篇精選文章 ---------- */
    .featured-posts {
      padding: clamp(56px, 8vw, 96px) 0 0;
      border-top: 1px solid var(--color-rule);
    }
    .featured-head {
      display: flex;
      align-items: end;
      justify-content: space-between;
      gap: 24px;
    }
    .featured-head > a {
      min-height: 44px;
      color: var(--color-accent);
      font-size: 13px;
      font-weight: 500;
      text-decoration: underline;
      text-underline-offset: 5px;
    }
    .featured-list {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: clamp(18px, 3vw, 34px);
      margin: 28px 0 0;
      padding: 0;
      list-style: none;
    }
    .featured-list li {
      min-width: 0;
      border-top: 1px solid var(--color-rule-2);
    }
    .featured-list a {
      display: flex;
      height: 100%;
      flex-direction: column;
      padding: 18px 0 0;
      color: inherit;
      text-decoration: none;
    }
    .featured-list img {
      display: block;
      width: 100%;
      height: auto;
      aspect-ratio: 1200 / 630;
      object-fit: cover;
      border: 1px solid var(--color-rule);
    }
    .featured-meta {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      margin-top: 14px;
      font-family: var(--font-mono);
      font-size: 10px;
      line-height: 1.5;
      color: var(--color-ink-3);
    }
    .featured-meta span {
      color: var(--color-accent);
    }
    .featured-list h3 {
      margin-top: 8px;
      font-size: 15px;
      line-height: 1.65;
      font-weight: 600;
      color: var(--color-ink);
      transition: color 0.18s var(--ease);
    }
    .featured-go {
      margin-top: auto;
      padding-top: 14px;
      font-size: 12px;
      color: var(--color-accent);
    }
    @media (max-width: 767px) {
      .service-details {
        padding-top: 64px;
      }
      .service-row {
        padding: 48px 0;
      }
      /* 手機版把文案容器攤平成 service-row 的 grid 子項，才能在不複製圖片的
         前提下，讓媒體落在「介紹與重點」之後、「查看連結」之前。桌機仍保留
         service-copy／media 左右雙欄，不受這組順序影響。 */
      .service-row .service-copy {
        display: contents;
      }
      .service-row .service-kicker {
        order: 1;
        margin-bottom: -18px;
      }
      .service-row .service-copy h2 {
        order: 2;
      }
      .service-row .service-copy > p:not(.service-kicker) {
        order: 3;
        margin-top: -10px;
      }
      .service-row .service-copy ul {
        order: 4;
        margin-top: -10px;
      }
      .service-row .service-media {
        order: 5;
        aspect-ratio: 16 / 10;
      }
      .service-row .service-link {
        order: 6;
        margin-top: -10px;
      }
      .case-mini-grid {
        order: 5;
      }
      .case-mini-grid li {
        grid-template-columns: 32px minmax(0, 1fr);
        min-height: 112px;
        padding: 12px;
      }
      .case-mini-icon {
        width: 32px;
        height: 32px;
      }
      .case-mini-grid strong {
        font-size: 12px;
      }
      .instructor-profile {
        grid-template-columns: minmax(0, 1fr);
        gap: 24px;
        padding: 56px 0;
      }
      .instructor-photo {
        width: min(74vw, 280px);
        aspect-ratio: 4 / 5;
      }
      .instructor-intro {
        font-size: 14px;
      }
      .instructor-facts {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
      .instructor-facts div {
        padding: 12px 10px 14px 0;
      }
      .instructor-facts div + div {
        padding-left: 10px;
        border-left: 1px solid var(--color-rule);
      }
      .instructor-facts dd {
        font-size: 11px;
      }
      .featured-posts {
        padding-top: 56px;
      }
      .featured-head {
        align-items: end;
      }
      .featured-list {
        grid-template-columns: minmax(0, 1fr);
        gap: 30px;
        margin-top: 22px;
      }
    }

    /* ---------- 結尾 CTA ---------- */
    .closer {
      padding-top: 76px;
    }
    @media (min-width: 768px) {
      .closer {
        padding-top: 120px;
      }
    }
    .closer-p {
      margin-top: 18px;
      font-size: 15.5px;
      line-height: 1.95;
      color: var(--color-ink-2);
      max-width: 52ch;
    }

    /* ============================================================
       hover 一律加裝置條件：觸控裝置上 :hover 會「黏住」，
       點過的元件會停在 hover 狀態直到點別的地方。
       :focus-visible 另外寫一份、不設任何條件。
       ============================================================ */
    @media (hover: hover) and (pointer: fine) {
      .btn-line:hover {
        background: #05b34c;
        border-color: #05b34c;
        transform: translateY(-1px);
      }
      .btn-contact:hover {
        color: var(--color-accent);
        border-color: var(--color-accent);
        background: rgb(255 255 255 / 64%);
        transform: translateY(-1px);
      }
      .instructor-socials a:hover {
        color: var(--color-accent);
        border-color: var(--color-accent);
        background: rgb(255 255 255 / 60%);
      }
      .featured-list a:hover h3 {
        color: var(--color-accent);
      }
      .cli-options button:hover {
        border-color: var(--color-accent-line-2);
        background: var(--color-accent-tint);
        color: var(--color-accent-2);
        box-shadow: 0 4px 10px rgb(122 46 59 / 10%);
        transform: translateY(-1px);
      }
      .cli-answer a:hover { color: var(--color-accent-2); }
      .cli-end:hover { color: var(--color-accent-2); }
      :global(.cli-search-result:hover) {
        border-color: var(--color-accent-line-2);
        background: var(--color-accent-tint);
      }
      .cli-search-form button:hover {
        border-color: var(--color-accent-2);
        background: var(--color-accent-2);
        color: var(--color-paper);
      }
    }

    /* 鍵盤焦點：不靠 hover 也要有等價的視覺回饋（不設裝置條件） */
    .instructor-socials a:focus-visible,
    .featured-list a:focus-visible,
    .cli-options button:focus-visible,
    .cli-answer a:focus-visible,
    .cli-end:focus-visible,
    :global(.cli-search-result:focus-visible),
    .cli-search-form button:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 3px;
    }
    .cli-search-form:focus-within {
      box-shadow: inset 3px 0 var(--color-accent);
    }
    .hero-contact-actions a:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 3px;
    }

    /* ============================================================
       逐段浮現：內容一律寫在 HTML 裡，只有「顯示與否」交給 CSS。
       沒有 JS 時 .js 不會加上，全部維持完整可見。
       IntersectionObserver 掛掉時另有 2 秒逾時強制顯示（見上方 script）。
       ============================================================ */
    .home.js .reveal {
      opacity: 0.3;
      transform: translateY(30px);
      transition:
        opacity 0.6s var(--ease) var(--d, 0ms),
        transform 0.6s var(--ease) var(--d, 0ms);
    }
    .home.js .reveal.is-in {
      opacity: 1;
      transform: translateY(0);
    }

    /* 首屏：0 + 70 + 140ms 延遲，各 200ms —— 總揭露 340ms */
    .home.js .hero-reveal {
      opacity: 0.3;
      transform: translateY(30px);
      transition:
        opacity 0.2s var(--ease) var(--d, 0ms),
        transform 0.2s var(--ease) var(--d, 0ms);
    }
    .home.js .hero-reveal.is-in {
      opacity: 1;
      transform: translateY(0);
    }

    @media (prefers-reduced-motion: reduce) {
      .home.js .reveal,
      .home.js .hero-reveal,
      .home.js .reveal.is-in,
      .home.js .hero-reveal.is-in {
        opacity: 1 !important;
        transform: none !important;
        transition: none !important;
      }
      /* 只關過渡，不寫任何 :hover 選擇器。
         「所有 hover 收在 @media (hover:hover) and (pointer:fine) 裡」是硬規則，
         在這裡補一條 .btn-line:hover{transform:none} 會是全站唯一一條沒有裝置
         條件的 hover 規則。而且它也沒有必要：那顆按鈕的 translateY(-1px) 本來就
         只在有滑鼠的裝置上生效，過渡一關就是瞬間位移，不構成前庭風險的動態。 */
      .btn,
      .cli-caret,
      .cli-thinking span,
      .terminal-typed,
      .terminal-command > i,
      .terminal-thinking,
      .terminal-done,
      .terminal-thinking i {
        transition: none !important;
        animation: none !important;
      }
      .cli-caret { display: none; }
      .terminal-typed {
        max-width: 18em;
      }
      .terminal-command > i,
      .terminal-thinking {
        display: none;
      }
      .terminal-done {
        opacity: 1;
        transform: none;
      }
      .cli-caret,
      .cli-thinking > span,
      .cli.is-ready .cli-options,
      .cli-answer {
        animation: none !important;
        transition: none !important;
      }
    }
  </style>
</Base>
